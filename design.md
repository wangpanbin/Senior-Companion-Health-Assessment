# design.md · 银龄伴诊 前端设计规范（Design Spec v1.0）

> **文档定位**：本文件是「银龄伴诊」前端的**视觉与交互唯一真源**。任何页面开发、组件封装、样式令牌取值，一律以本文件为准。
> **上游依据**：`docs/prd/PRD.md`（产品需求）· `plan.md`（工程拆分）· `AGENTS.md`（编码规范）
> **视觉依据**：竞讲原型板 `银龄伴诊-原型总览.png`（M11/M3/M4/M5/M6/M8/M9/M10 十个板块）
> **适用对象**：前端 C 位（UI）为主，A 位（前端主力）共同遵守
> **版本**：v1.0 · 2026-09-16 · 随 PRD 版本锁（W7 / W12 / W17）同步修订

---

## 0 · 一页速览

| 维度 | 结论 |
|---|---|
| 风格关键词 | 医疗蓝 · 大面积留白 · 圆角卡片 · 轻投影 · 高信息密度但零噪音 |
| 主色 | `#1D6FF2`（医疗蓝） |
| 底色 | `#F4F7FB`（冷白蓝底），卡片纯白 |
| 字体 | Noto Sans SC（中文）/ Inter（数字·英文）；数字统一用 Inter 保证等宽对齐 |
| 圆角 | 卡片 14 · 按钮 12 · 标签 6 · 底部胶囊 36 |
| 阴影 | 单层轻投影，禁止重阴影 / 拟态 |
| 最小触控区 | 普通 44px · **老人模式 48px（硬约束）** |
| 基线字号 | 普通 14px · **老人模式 ≥18px（硬约束）** |
| 移动端画布 | 390 × 844（含 62px 状态栏 + 62px 胶囊 Tab Bar） |
| 管理后台画布 | 1440 × 900（左栏 220 + 内容区） |
| 图标 | 全部 SVG 线性图标，24×24 网格，stroke 1.8；**禁止 emoji / 图标字体** |
| 图表 | ECharts 5，主色单色渐变，不做彩虹配色 |

---

## 1 · 页面规划（Page Inventory）

### 1.1 优先级定义

- **P0**：竞讲现场必演，本轮优先出高保真稿（共 13 屏）
- **P1**：功能完整度必需，第二轮补齐
- **P2**：边缘/后台低频页，可用 Element Plus 默认样式快速落地

### 1.2 移动端（老人 / 家属 / 陪诊员）

| 编号 | 页面 | 对应模块 | 核心角色 | 优先级 | 关键要素 |
|---|---|---|---|---|---|
| M-01 | 登录 | M2 | 全角色 | P1 | 账号+密码+图形验证码；失败 5 次锁 15 分钟提示 |
| M-02 | 注册（角色分流） | M2 | FAMILY / COMPANION | P1 | 角色二选一卡片；陪诊员注册后引导去资质提交 |
| M-03 | 忘记密码 / 重置 | M2 | 全角色 | P2 | 手机验证码 + 新密码 |
| **M-04** | **老人端首页** | M11 / M3 | ELDER | **P0** | 今日用药提醒卡 · 陪诊员实时位置卡 · 适老化模式切换 |
| **M-05** | **老人端首页 · 老人模式** | M11 | ELDER | **P0** | 与 M-04 同结构，全局字号 ×1.25、对比度加深、菜单精简 |
| **M-06** | **家属端首页** | M3 | FAMILY | **P0** | 绑定老人卡（多老人横滑）· 预约挂号/用药计划/进度查看 · 订单进行中卡 |
| **M-07** | **下单 Step1 · 选择就诊人** | M4 | FAMILY | **P0** | 老人列表单选 · 底部「下一步」 |
| **M-08** | **下单 Step2 · 选择医院与时间** | M4 | FAMILY | **P0** | 医院列表（含距离文字）+ 日期横向选 + 时段网格 |
| **M-09** | **下单 Step3 · 确认订单** | M4 | FAMILY | **P0** | 就诊人 / 医院 / 时间 / 服务地址（文字，无地图）/ 服务费 → 提交订单；**固定文案「本服务为线下结算」** |
| **M-10** | **陪诊员端 · 接单大厅** | M4 | COMPANION | **P0** | 资质状态条 · 筛选（全部/附近/高额）· 订单卡「抢单」；乐观锁失败提示 |
| **M-11** | **陪诊员端 · 订单执行打卡** | M5 | COMPANION | **P0** | 6 节点时间线（出发/到院/就诊中/取药/离院/完成）· 当前位置 + 距离阈值提示 |
| **M-12** | **用药管理 · 日历视图** | M6 | FAMILY / COMPANION | **P0** | 月历 + 每日服药圆点标记 + 今日用药列表 |
| **M-13** | **用药计划详情 / 确认服药** | M6 | FAMILY / COMPANION | **P0** | 药品卡（名称/规格/通用说明）· 已服/漏服确认 · **底部固定免责声明** |
| **M-14** | **站内信与通知** | M8 | 全角色 | **P0** | 4 类模板分组（订单事件/资质审核/漏服提醒/系统公告）· 未读红点 · 全部已读 |
| M-15 | 订单列表 | M4 | ELDER / FAMILY / COMPANION | P1 | 状态分段器（全部/待接单/服务中/已完成/已取消） |
| M-16 | 订单详情 + 实时时间线 | M4 / M5 | 全角色 | P1 | WebSocket 实时打卡推送 · 订单状态流水 · 费用明细（线上记账） |
| M-17 | 评价订单 | M7 | FAMILY | P1 | 1-5 星 + 标签多选 + 文字 + 匿名开关；仅 COMPLETED 可见入口 |
| M-18 | 投诉 / 我的投诉 | M7 | 全角色 | P1 | 3 状态（待处理/处理中/已结案）· 敏感词拦截提示 |
| M-19 | 老人档案 / 绑定老人 | M3 | FAMILY | P1 | 邀请码绑定 · 身份证号脱敏展示 · 新增/编辑 |
| M-20 | 陪诊员入驻 · 资质提交 | M3 | COMPANION | P1 | 身份证/健康证上传（≤10MB）· 区域时段偏好 · 提交后待审态 |
| M-21 | 陪诊员 · 我的接单与收入 | M4 | COMPANION | P1 | 本周接单数/评分/收入排行 · 服务费账单 |
| M-22 | 我的 / 个人资料 | M2 | 全角色 | P1 | 角色标识 · 老人模式开关 · 退出登录 |
| M-23 | 空状态 / 加载 / 错误 | 全局 | 全角色 | P1 | 三态统一组件（含 403 无权限、404、网络异常） |

### 1.3 管理后台（桌面端，ADMIN 专属）

| 编号 | 页面 | 对应模块 | 优先级 | 关键要素 |
|---|---|---|---|---|
| W-01 | 数据看板 | M10 | **P0** | 订单趋势折线 · 完成率环形 · 陪诊员排行 Top10 · 漏服率 · 用户增长 |
| W-02 | 陪诊员资质审核 | M9 | **P0** | 列表 + 证件预览 + 通过/驳回（驳回必填原因） |
| W-03 | 订单纠纷仲裁 | M9 | **P0** | 订单时间线 + 双方材料 + 强制改终态 + 双方通知 |
| W-04 | 用户管理 | M9 | P1 | 封禁/解封/重置密码 · 手机号脱敏 |
| W-05 | 订单管理 | M9 | P1 | 全量订单筛选 + 状态流转查看 |
| W-06 | 投诉管理 | M7 / M9 | P1 | 3 状态流转 · 全程留痕 |
| W-07 | 统计导出 | M10 | P1 | 按当前筛选条件导出 Excel（不含密码/身份证/完整手机号） |
| W-08 | 操作日志 | M9 | P2 | `admin_oper_log` 只读查询 |

### 1.4 路由与可见性（`router/routes.js` meta 约定）

```js
{ path: '/elder/home', meta: { title: '首页', icon: 'home', roles: ['ELDER'], elderlyHidden: false, module: 'M11' } }
{ path: '/companion/hall', meta: { title: '接单大厅', icon: 'list', roles: ['COMPANION'], elderlyHidden: true, module: 'M4' } }
{ path: '/admin/**', meta: { title: '管理后台', roles: ['ADMIN'], elderlyHidden: true, module: 'M9' } }
```

- `elderlyHidden: true` 的路由在老人模式下**从菜单中移除**（M-10 / M-11 陪诊员端、W-* 后台、M-20 资质入驻、所有统计图表页）。
- `roles` 用于路由守卫；**前端隐藏不等于安全边界**，服务端 `@PreAuthorize` 才是。

---

## 2 · 设计令牌（Design Tokens）

> 落地方式：`frontend/src/styles/variables.scss`（编译期 SCSS 变量）+ `styles/index.scss`（运行期 CSS 自定义属性）。
> 命名前缀统一 `--nl-`。

### 2.1 色彩

```scss
/* ---------- 品牌色 ---------- */
--nl-primary:        #1D6FF2;   /* 主色：按钮 / 选中 / 强调 / 链接 */
--nl-primary-dark:   #1557C8;   /* 按压态 / 老人模式主色（对比度加深） */
--nl-primary-light:  #E8F1FE;   /* 浅蓝底：选中背景 / 标签底 / 图标底 */
--nl-primary-ghost:  #F4F8FF;   /* 极浅蓝底：区块分隔 / 悬浮行 */
--nl-primary-gradient: linear-gradient(135deg, #3D8BFF 0%, #1D6FF2 100%);

/* ---------- 中性色 ---------- */
--nl-bg:             #F4F7FB;   /* 页面底色 */
--nl-bg-card:        #FFFFFF;   /* 卡片 */
--nl-bg-sunken:      #F8FAFD;   /* 内嵌区 / 输入框底色 */
--nl-bg-mask:        rgba(17, 26, 41, 0.45);  /* 遮罩 */

--nl-border:         #E7EDF5;   /* 常规边框 */
--nl-border-strong:  #D5DFEC;   /* 强调边框 / 分页器 */
--nl-divider:        #EFF3F8;   /* 列表分隔线 */

/* ---------- 文字 ---------- */
--nl-text-1:         #17202F;   /* 标题 / 主文（对比度 14.8:1） */
--nl-text-2:         #5B6B84;   /* 次要说明（对比度 5.6:1） */
--nl-text-3:         #93A1B5;   /* 弱化 / 占位 / 禁用（仅限非关键信息） */
--nl-text-inverse:   #FFFFFF;
--nl-text-link:      #1D6FF2;

/* ---------- 语义色 ---------- */
--nl-success:        #17B26A;   --nl-success-bg: #E7F8F0;   /* 已服 / 已完成 / 审核通过 / 已结案 */
--nl-warning:        #F79009;   --nl-warning-bg: #FEF3E2;   /* 待接单 / 待处理 / 待审 / 漏服临近 */
--nl-danger:         #F04438;   --nl-danger-bg:  #FEECEB;   /* 漏服 / 已取消 / 驳回 / 封禁 */
--nl-info:           #1D6FF2;   --nl-info-bg:    #E8F1FE;   /* 服务中 / 处理中 */
--nl-purple:         #7A5AF8;   --nl-purple-bg:  #F0EDFE;   /* 已接单（与 info 区分） */
--nl-neutral-chip:   #F1F4F9;   /* 中性标签底，文字 --nl-text-2 */
```

### 2.2 状态色映射（**开发直接抄这张表**）

| 业务状态 | 枚举 | 文字色 | 底色 |
|---|---|---|---|
| 待接单 | `PENDING` | `--nl-warning` | `--nl-warning-bg` |
| 已接单 | `ACCEPTED` | `--nl-purple` | `--nl-purple-bg` |
| 服务中 | `IN_SERVICE` | `--nl-info` | `--nl-info-bg` |
| 已完成 | `COMPLETED` | `--nl-success` | `--nl-success-bg` |
| 已评价 | `REVIEWED` | `--nl-text-2` | `--nl-neutral-chip` |
| 已取消 | `CANCELLED` | `--nl-danger` | `--nl-danger-bg` |
| 用药·待服 | `PENDING` | `--nl-text-2` | `--nl-neutral-chip` |
| 用药·已服 | `TAKEN` | `--nl-success` | `--nl-success-bg` |
| 用药·漏服 | `MISSED` | `--nl-danger` | `--nl-danger-bg` |
| 资质·待审 | `PENDING` | `--nl-warning` | `--nl-warning-bg` |
| 资质·通过 | `APPROVED` | `--nl-success` | `--nl-success-bg` |
| 资质·驳回 | `REJECTED` | `--nl-danger` | `--nl-danger-bg` |
| 投诉·待处理 | `PENDING` | `--nl-warning` | `--nl-warning-bg` |
| 投诉·处理中 | `PROCESSING` | `--nl-info` | `--nl-info-bg` |
| 投诉·已结案 | `CLOSED` | `--nl-success` | `--nl-success-bg` |
| 用户·正常 | `ACTIVE` | `--nl-success` | `--nl-success-bg` |
| 用户·封禁 | `BANNED` | `--nl-danger` | `--nl-danger-bg` |

> **中国股市配色规则不适用于本项目**；本项目语义色遵循「绿=正常完成、橙=待办、红=异常」，与医疗系统惯例一致。

### 2.3 间距与圆角

```scss
/* 间距阶梯（4 的倍数） */
--nl-space-1: 4px;  --nl-space-2: 8px;   --nl-space-3: 12px;
--nl-space-4: 16px; --nl-space-5: 20px;  --nl-space-6: 24px;  --nl-space-8: 32px;

/* 页面级 */
--nl-gutter: 16px;    /* 移动端页面左右内边距（wrapper 统一施加） */
--nl-gap-section: 20px;  /* 区块之间 */
--nl-gap-item: 12px;     /* 区块内条目之间 */

/* 圆角 */
--nl-radius-card: 14px;   --nl-radius-btn: 12px;
--nl-radius-chip: 6px;    --nl-radius-input: 12px;
--nl-radius-pill: 999px;  --nl-radius-phone: 44px;
```

### 2.4 阴影（**只允许这三档**）

```scss
--nl-shadow-card:  0 2px 8px rgba(23, 43, 77, 0.06);
--nl-shadow-hover: 0 6px 16px rgba(23, 43, 77, 0.08);
--nl-shadow-float: 0 8px 24px rgba(23, 43, 77, 0.12);   /* 底部 Tab Bar / 弹层 */
```

禁止：拟态（neumorphism）、发光（glow）、多层重投影、彩色阴影。

### 2.5 字体与字号阶梯

```scss
--nl-font-cn: "Noto Sans SC", "PingFang SC", "Microsoft YaHei", sans-serif;
--nl-font-num: "Inter", "SF Pro", system-ui, sans-serif;   /* 数字/金额/时间强制使用 */
```

| Token | 字号/行高 | 字重 | 用途 |
|---|---|---|---|
| `--nl-font-display` | 30 / 38 | 700 | 页面级大数字（收入、评分） |
| `--nl-font-h1` | 22 / 30 | 600 | **页面标题（全站统一，禁止逐页变化）** |
| `--nl-font-h2` | 18 / 26 | 600 | 卡片标题 |
| `--nl-font-h3` | 16 / 24 | 500 | 列表主文 |
| `--nl-font-body` | 14 / 22 | 400 | 正文 / 说明 |
| `--nl-font-caption` | 12 / 18 | 400 | 辅助信息 / 时间戳 |
| `--nl-font-micro` | 10 / 14 | 500 | Tab Bar 标签（大写字母间距 0.5px） |

**数字规范**：金额、距离、评分、日期时间一律 `--nl-font-num` + `font-variant-numeric: tabular-nums`，保证右对齐与等宽。

### 2.6 尺寸硬约束

| 项 | 值 | 出处 |
|---|---|---|
| 状态栏高度 | 62px（内容垂直居中） | 视觉规范 |
| 顶部导航栏 | 56px（标题 22/600） | 视觉规范 |
| 主按钮高度 | 48px（老人模式 56px） | 适老化 ≥48px |
| 输入框高度 | 48px | 适老化 |
| 列表行最小高度 | 56px（老人模式 68px） | 适老化 |
| 胶囊 Tab Bar | 容器 12/21 padding，胶囊高 62、圆角 36，图标 18、标签 10 | 视觉规范 |
| 移动端画布 | 390 × 844 | iPhone 14 |
| 单文件上传上限 | 10 MB（前端预校验，超限不发起请求） | PRD §8.3 |
| 分页 size 上限 | 100（前端不传 >100 的值） | PRD §8.3 |

---

## 3 · 组件规范

### 3.1 按钮

| 类型 | 高度 | 底色 | 文字 | 圆角 |
|---|---|---|---|---|
| Primary | 48 | `--nl-primary`（按压 `--nl-primary-dark`） | `#FFF` 16/500 | 12 |
| Secondary | 48 | `#FFF` + 1px `--nl-primary` | `--nl-primary` 16/500 | 12 |
| Ghost | 40 | 透明 | `--nl-primary` 14/500 | 10 |
| Danger | 48 | `--nl-danger` | `#FFF` 16/500 | 12 |
| Disabled | — | `#F1F4F9` | `--nl-text-3` | — |

- 底部主行动按钮：`width: fill_container`，页面底部安全区内边距 16px。
- 同一屏**只允许一个 Primary**，禁止并列两个主按钮。

### 3.2 卡片

```
卡片 = 白底 + 1px --nl-border + radius 14 + padding 16 + 内部 gap 12 + --nl-shadow-card
```

- 卡片标题行：`h3(16/500)` 左 + 右侧操作（`caption` 文字或 chevron 图标）。
- 卡片内**禁止**再嵌套投影卡片（层级最多 2 层：底 → 卡）。

### 3.3 状态标签（Chip）

高度 22 · 圆角 6 · padding 0 8 · 字号 12/500 · 底色/文字色取 §2.2 映射表。

### 3.4 列表行（ListRow）

```
[图标容器 36×36 圆角 10 浅蓝底 + 20px SVG] ← gap 12 → [标题 16/500 + 副标题 13/400 --nl-text-2] ← SPACE_BETWEEN → [chevron 16px --nl-text-3]
```
最小高度 56（老人模式 68），分隔线 `--nl-divider` 1px，首行去线、末行去线。

### 3.5 底部胶囊 Tab Bar

- 容器：全宽 · padding `12px 21px 21px`（含home indicator 安全区）· 底色白 + `--nl-shadow-float`
- 胶囊：高 62 · `fill_container` 宽 · 圆角 36 · 1px `--nl-border` · 内 padding 4
- Tab 项：等分宽高 · 圆角 26 · 竖向 gap 4 · 图标 18 + 标签 10/500
- **激活态**：实心 `--nl-primary` 底 + 白色图标文字（不是只换颜色）
- **非激活态**：透明底 + `--nl-text-3` 图标文字
- Tab 数量 3–5。老人端 4 个（首页/用药/消息/我的），家属端 5 个（首页/订单/用药/消息/我的），陪诊员端 4 个（大厅/订单/收入/我的）。

### 3.6 状态三件套（必须实现）

| 状态 | 表现 |
|---|---|
| 加载中 | 骨架屏（卡片轮廓 + 灰色渐变扫光），**禁用整页转圈** |
| 空数据 | 80px 线性插画 + 「暂无数据」+ 一句行动引导（如「去接单大厅看看」） |
| 错误 | 分类型：403 → 「无权限访问」；404 → 「页面不存在」；网络 → 「网络开小差了」+ 重试按钮 |

### 3.7 反馈

- Toast：顶部滑入，2.5s 自动消失；成功绿底图标、失败红底图标。
- 二次确认：删除 / 取消订单 / 驳回资质 / 封禁 / 强制改终态，**必须**弹确认（写清后果）。
- 乐观锁失败（抢单 3003）：Toast「手慢了，订单已被接走」+ 自动刷新列表。

---

## 4 · 移动端页面骨架（Screen Blueprint）

> 每屏统一按此骨架搭建，**所有内容必须在一个 wrapper 纵向栈内**（`gap: 20`，左右 padding 16），禁止逐块自行加左右边距。

1. **状态栏**（62px）：左侧时间（Inter 15/600）+ 右侧信号/WiFi/电量 SVG 图标
2. **顶部导航栏**（56px）：返回箭头（可选）+ 标题（22/600，居左）+ 右侧操作图标（可选）
3. **内容区**（可滚动，单列纵向）：主要意图 → 次要模块 → 帮助/免责文案
4. **底部**：胶囊 Tab Bar（主页面）／ 主行动按钮（流程页，无 Tab Bar）
5. 底部留白 = 内容区 `gap` 值（20px），禁止用空白占位元素

### 逐屏要点

**M-04 / M-05 老人端首页（M11 核心）**
- 问候卡：「张三爷，早上好」+ 今日日期 + 健康评分徽标；底为极浅蓝渐变。
- 今日用药提醒卡：时间胶囊（08:00 / 12:00 / 20:00）+ 药品名 + 状态 chip（已服 / 待服 / 漏服）。
- 陪诊员实时位置卡：头像 + 姓名 + 「已在路上 预计 10 分钟到达」（**文字描述，无地图**）+ Primary 按钮「查看详情」。
- 老人模式（M-05）：字号 ×1.25、正文 18、按钮高 56、主色 `--nl-primary-dark`、导航栏仅保留「首页/用药/消息/我的」。

**M-06 家属端首页（M3）**
- 老人卡横向滑动（多头像 + 姓名 + 年龄 + 健康徽标 + 绑定人数角标）。
- 三宫格快捷入口：预约挂号 / 用药计划 / 进度查看（浅蓝图标底 48×48）。
- 进行中订单卡：状态 chip + 医院 + 时间 + 陪诊员头像 + 「查看详情」。
- 关爱之家 banner：一句话价值主张 + 「专业·安心·便捷」标签。

**M-07 → M-09 下单三步（M4）**
- 顶部三步步骤条：圆形序号 + 连线，已完成态实心主色、当前态描边主色、未开始态灰。
- Step1 单选老人卡（选中态 1px 主色边 + 浅蓝底 + 右上勾选圆点）。
- Step2 医院列表（名称 + 「三甲」chip + 距离文字如「2.1km」，**无地图**）；日期横向 7 天；时段 3×2 网格（选中态主色底白字、已满置灰不可点）。
- Step3 信息确认行（就诊人/医院/时间/服务地址/服务费）+ 顶部提示条「本服务为线下结算」+ 底部 Primary「提交订单」+ Ghost「返回修改」。

**M-10 陪诊员端接单大厅（M4）**
- 顶部资质状态条：已认证 chip + 「接单中」开关。
- 分段筛选：全部 / 附近 / 高额。
- 订单卡：医院名 + `display` 级服务费（`¥120`）+ 时间 + 距离文字 + 右侧 Primary「抢单」；抢单成功 → 卡片切换为「已接单」并置顶。
- 未通过资质：整页拦截态 + 「去提交资质」按钮。

**M-11 陪诊员端订单执行（M5）**
- 订单摘要头（医院 + 时段 + 距离）。
- 6 节点竖向时间线：左侧圆点（已完成实心主色 / 当前脉冲描边 / 未开始灰）+ 竖线 + 右侧节点名 + 完成时间戳；**仅当前节点可点，不可回退**。
- 锁定节点显示「请先完成上一节点」。
- 底部定位条：「当前位置 · 北京市朝阳区xx路12号」+「定位正常 / 偏离超过 2000 米」提示（越界则 Primary 置灰并说明原因）。

**M-12 / M-13 用药管理（M6）**
- 日历：月标题 + 前后切换、周一为起始、日格内 3 个状态圆点、今天描边主色。
- 今日用药卡：药品icon + 药名（h3）+ 规格/通用说明（caption）+ 频次 chip + 服用时段文字 + 状态 chip。
- 确认动作：Primary「确认已服」；已服后转绿底只读态 + 确认时间。
- **页面底部固定免责声明条**（不可关闭）：`本页仅提供药品通用信息，不构成任何用药建议，请遵医嘱。`

**M-14 站内信（M8）**
- 顶部「消息（3 条未读）」+ 右侧「全部已读」Ghost。
- 按 4 类模板分组标题：订单事件 / 资质审核 / 漏服提醒 / 系统公告。
- 列表行：类型图标（36 圆角底）+ 标题 + 摘要 + 右侧时间 + 未读蓝点。
- 空态：「暂无新消息」。

### 管理后台骨架（W-*）

```
左侧栏 220px（Logo + 菜单，选中态左侧 3px 主色条 + 浅蓝底）
└ 内容区：顶部 64px（面包屑 + 管理员信息）→ 内容 padding 24 → 筛选条 / 表格 / 图表卡
```

- 表格：表头 `--nl-bg-sunken`、行高 48、hover `--nl-primary-ghost`、斑马纹关闭、分页器右对齐。
- 敏感列（手机号 / 身份证号）**直接展示后端返回的脱敏串**，前端不再二次处理、也不做「点击查看完整」的入口。
- 看板图表：订单趋势（折线+面积渐变）、完成率（环形）、陪诊员排行 Top10（横向条 + 前三名奖牌色）、漏服率（折线）。
- 危险操作（封禁 / 强制改终态 / 驳回）按钮为 `--nl-danger` 文字按钮，点击后二次确认弹窗写明「将通知双方」。

---

## 5 · 适老化规范（M11 · 硬约束）

| 项 | 普通模式 | 老人模式 |
|---|---|---|
| 触发 | `document.documentElement.classList.remove('elderly-mode')` | `...add('elderly-mode')`，无需刷新 |
| 字阶 | 14 / 16 / 18 / 22 | **×1.25** → 18 / 20 / 22 / 26（基线 ≥18） |
| 触控区 | 44px | **≥48px**，主按钮 56px |
| 主色 | `#1D6FF2` | `#1557C8`（对比度 ≥ 4.5:1） |
| 正文色 | `#17202F` | `#111827`（纯黑系，去灰） |
| Tab Bar | 图标 18 / 标签 10 | 图标 22 / 标签 14 |
| 导航栏 | 56px | 64px |
| 菜单 | 全量 | 隐藏 `meta.elderlyHidden` 路由（陪诊员端/后台/统计图表/资质入驻） |
| 交互 | 允许二级筛选 | 复杂筛选折叠为「更多条件」，核心流程 ≤ 3 步 |

实现落点：`styles/elderly.scss` 用 `.elderly-mode { --nl-font-body: 18px; ... }` 覆盖 CSS 变量，组件层零改动。持久化键 `nl_elderly_mode`（值 `'1'`/`'0'`）。

---

## 6 · 合规红线在 UI 上的落地（**不可突破**）

1. **不做诊断、不开药方**
   - 药品详情/用药卡**禁止**出现：`建议服用`、`推荐剂量`、`诊断为`、`可能是·病`、`对症`、`可替代`、`医嘱建议`。
   - 剂量输入框的 label 固定为「家属录入剂量」，并附 caption「剂量由家属按医嘱录入，系统不提供建议」。
   - 每个含药品信息的页面底部必须有固定免责声明条。
   - 全站 grep 自检：`建议服用|推荐剂量|诊断为|对症|可替代` 结果必须为空。

2. **隐私最小化**
   - 手机号、身份证号**直接渲染后端返回的脱敏串**（`138****8888` / `110101********4567`），前端不持有明文。
   - `localStorage` 白名单仅允许：`nl_token`、`nl_user_id`、`nl_role`、`nl_elderly_mode`、`nl_nickname`。
   - **禁止**写入：密码、身份证号（含加密串）、完整手机号。
   - 前端日志、埋点、错误上报**禁止**携带任何身份证字段。

3. **一期不做（UI 上也不得出现入口）**
   - 无「立即支付」按钮 → 统一为「线上记账 · 线下结算」账单视图，附文案「本服务为线下结算」。
   - 无地图组件 → 统一为「文字地址 + 签到坐标文本」，坐标以 `经度 116.46 / 纬度 39.92` 形式展示。
   - 无 IM → 统一为站内信（单向通知），消息页不出现输入框与「发送」按钮。

4. **老人账号默认只读（ELDER）**
   - 老人端全部页面为只读视图：不出现下单、评价、确认服药、抢单、提交资质等写操作按钮。
   - 涉及代操作处显示说明条：「此操作需由家属完成」（服务端同样 403 兜底）。

---

## 7 · 前端工程落地约定

| 项 | 约定 |
|---|---|
| 令牌文件 | `frontend/src/styles/variables.scss`（SCSS 变量，编译期）+ `styles/index.scss`（`:root` CSS 变量，运行期） |
| 运行期主题 | 所有颜色/字号走 CSS 变量，**组件内禁止硬编码色值**（老人模式靠变量覆盖实现） |
| 老人模式样式 | `styles/elderly.scss`，仅覆盖变量，不写组件选择器 |
| 组件目录 | `frontend/src/components/` 下按 `nl-` 前缀：`nl-button / nl-card / nl-chip / nl-list-row / nl-tabbar / nl-navbar / nl-steps / nl-timeline / nl-empty / nl-skeleton` |
| 图标 | `frontend/src/components/icons/` 存 SVG 源文件，统一 24×24 viewBox、`stroke: currentColor`、`stroke-width: 1.8`、`stroke-linecap: round` |
| 请求层 | 统一走 `utils/request.js`；错误码映射到 §3.7 反馈策略；401 跳登录、403 走无权限页 |
| 单位 | 布局用 `px`（移动端不做 rem 缩放），字号用 CSS 变量 |
| 断点 | `<768px` 移动端；`≥768px` 平板/桌面（仅后台使用） |

---

## 8 · 禁止项清单（AI 与人工同样适用）

- ❌ Emoji 或 Unicode 符号充当图标（`✓` `▶` `🏥` 一律禁止，必须用 SVG）
- ❌ 图标字体 / `icon_font`
- ❌ 硬编码色值（`#fff` 直接写在组件里）
- ❌ 彩虹配色、多主色并存（全站仅一个品牌蓝）
- ❌ 重投影、拟态、发光、超过 2 层的嵌套卡片
- ❌ 一屏并列两个 Primary 按钮
- ❌ 逐页不同的标题字号
- ❌ 移动端使用表格、多级下拉筛选（老人端完全禁止）
- ❌ 在 UI 上出现支付、地图、IM 入口（见 §6.3）
- ❌ 在 UI 上出现任何剂量建议类文案（见 §6.1）

---

## 9 · 交付物与轮次

| 轮次 | 内容 | 状态 |
|---|---|---|
| R1（本轮） | 本规范文档 + P0 高保真原型板（M-04~M-14 + W-01~W-03，共 13 屏） | ✅ 完成 |
| R2 | P1 页面补齐（M-01/02/15~M-22 + W-04~W-07）+ 三态组件 | 待启动 |
| R3 | 组件库代码化（`components/` 实装）+ 老人模式变量覆盖验收 | 待启动 |
| R4 | 全站合规 grep 自检 + 适老化对比度自动校验 | 待启动 |

**验收口径**：本规范中的令牌与组件尺寸，在浏览器 DevTools 中抽查 5 处必须与文档一致；任意页面的字号/触控区在 `elderly-mode` 下必须满足 §5 表格。

---

## 附 · 版本记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v1.0 | 2026-09-16 | 首版。基于 PRD v1.0 与竞讲原型板提取视觉规范，锁定令牌、组件、页面规划与合规落地口径 |
