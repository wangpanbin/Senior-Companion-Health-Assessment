# 银龄伴诊 — 项目开发计划书（模块拆分 + 环境需求）

> 本文档由《银龄伴诊项目计划书.docx》推导生成，用于把 17 周课程设计拆成**可独立分步执行的工程模块**，并给出**当前本机环境的缺口清单**。
>
> - 生成时间：2026-09-15
> - 来源文档：`F:\test\Senior Companion Health Assessment\银龄伴诊项目计划书.docx`
> - 项目根目录：`F:\test\Senior Companion Health Assessment`

---

## 〇 · ⚠️ 当前阶段范围调整（2026-09-16 更新）

**本阶段只做一件事：专注代码编写。** 以下内容**不纳入本阶段交付范围**，无论原计划书里怎么写。

| 不做的项 | 理由 | 替代兜底 |
|---|---|---|
| **项目部署上线**（Docker / docker-compose / deploy.sh / Nginx 反代 / 云服务器 / 部署文档） | 本机演示即可；容器化与部署不产生验收价值 | 本机 `mvn spring-boot:run` + Knife4j `/doc.html` |
| **答辩 PPT**（W17 的 3 份） | 时间点未到，现在做会随代码变动反复返工 | 等到 W17 启动前再做，本阶段不动 |
| **演示视频**（每迭代一段，3 段） | 非本阶段目标 | 同上 |
| **用户手册**（4 角色 × 5 张截图） | 依赖前端界面，而一期不推进前端 | 同上 |
| **全链路演练**（W16 前 24h） | 随部署一并排除 | — |

**本计划书的影响范围**：

1. 下面"模块拆分总览"表里 **M13「部署与交付物」一整行**标注为「⏸️ 现阶段排除」，**其下原文整章保留**仅作历史归档，便于以后重启时取用。
2. 各模块验收清单里提到「用户手册」「答辩 PPT」「演示视频」的条目，统一删除或加 ⏸️。
3. §四「环境风险与注意事项」里关于 Docker / WSL2 / BIOS 虚拟化 的整段**降级**为「历史记录」，不再作为待办缺口。
4. 配套文档同步：
   - `docs/agents/PLAN_BACKEND.md §0` 已写明本调整（后端视角）
   - `docs/adr/0004-m13-docker-fallback.md` 已置 `DEFERRED`
   - `docs/agents/AUDIT_BACKEND_2026-09-16.md §0/§3.2` 已体现

**重启条件**：若后续决定恢复 Docker 化部署 / 编写答辩 PPT，本计划书 M13 章节与 ADR-0004 候选方案可直接复用，无需重做设计。

---

## 一、项目基线（从计划书提取）

| 项 | 内容 |
|---|---|
| 项目名称 | 「银龄伴诊」—— 老年人就医陪诊与用药协同管理平台 |
| 一句话定位 | 让不会用手机的老人，也能有人陪着把病看好 |
| 团队 | 4 人（A 前端主力 / B 后端主力 / C 前端·UI 兼测试 / D 后端·DB 兼项目管理） |
| 周期 | 17 周，6 个迭代，第 4/7/12/13/16/17 周为竞讲交付节点 |
| 前端 | Vue 3 + Vite + Element Plus + Vue Router 4 + Pinia + Axios + ECharts |
| 后端 | Spring Boot 3.x + MyBatis-Plus 3.5+ + Spring Security + JWT + MySQL 8.0 + Maven |
| 中间件 | Redis（验证码 / 字典缓存 / 限流 / 分布式锁）、WebSocket 或 SSE |
| 文档 | Knife4j / Swagger、EasyExcel |
| 测试 | JUnit5 + Mockito + JMeter |
| ~~部署~~ | ~~Docker + docker-compose + deploy.sh~~ — ⏸️ 现阶段排除（见 §〇） |

### 四类角色（决定模块边界）

| 角色 | 画像 | 核心痛点 | 对应端 |
|---|---|---|---|
| 老年患者（60+） | 独居/空巢，智能机只会接电话 | 忘吃药、吃错药；找不到科室；没人陪检；听不懂医嘱 | 老人端（只读、极简） |
| 子女/家属（异地居多） | 30–45 岁上班族 | 不知道父母是否按时吃药；请不了假；想了解就诊结果 | 家属端（下单 + 代操作 + 看进度） |
| 陪诊员/社区志愿者 | 医院周边从业者或社区志愿者 | 微信群抢单、无凭证、无评价、无保险、纠纷多 | 陪诊员端（接单 + 打卡 + 结算） |
| 平台管理员 | 社区卫生服务中心/项目组运营 | 审核资质、处理投诉、统计数据 | 管理后台 |

### 硬性范围约束（计划书「合规与隐私说明」，所有模块必须遵守）

1. **不做诊断、不开药方** — 全部文案定位为「记录与提醒」，药品字典只给通用信息，页面必须有免责声明。
2. **隐私合规** — 密码 BCrypt 加密；手机号脱敏展示；接口最小化返回；日志禁止打印身份证号。
3. **一期砍掉**：在线支付（改「线上记账 + 线下结算」）、地图导航（改文字地址 + 签到坐标）、IM 聊天（改站内信）。

---

## 二、模块拆分总览

共 **14 个模块（M0–M13）**。每个模块可独立开发、独立验收、独立提交。

| 编号 | 模块 | 归属迭代 / 周次 | 主责 | 前置依赖 |
|---|---|---|---|---|
| M0 | 工程骨架与开发规范 | 迭代一 / W1 | A + B | — |
| M1 | 数据库设计与数据初始化 | 迭代一 / W2–W3 | D | M0 |
| M2 | 认证与多角色鉴权 | 迭代一·二 / W3–W4 | B | M0, M1 |
| M3 | 用户与档案管理 | W2–W5、W13 | B + D | M1, M2 |
| M4 | 陪诊订单与状态机 | 迭代二 / W5–W7 | B + A | M3 |
| M5 | 陪诊执行、打卡与实时进度 | 迭代三 / W8–W9 | A + B | M4 |
| M6 | 用药管理与漏服提醒 | 迭代三 / W9–W10 | B + A + D | M1, M8 |
| M7 | 评价与投诉 | 迭代三 / W10 | A + B | M4 |
| M8 | 站内信与通知 | W9–W12 | B | M2 |
| M9 | 管理后台 | 迭代四·五 / W13–W14 | A + B | M3, M4, M7 |
| M10 | 数据统计、可视化与导出 | 迭代五 / W14–W15 | A + B | M4, M9 |
| M11 | 适老化前端体系 | 横切 W3–W16 | A + C | M0 |
| M12 | 测试与质量保障 | 横切 W11 / W16 | C + B | M2–M10 |
| M13 | ⏸️ **部署与交付物**（现阶段排除） | ~~迭代五·六 / W15–W17~~ | — | — |

### 依赖拓扑（实施顺序）

```
M0 ──► M1 ──► M2 ──► M3 ──► M4 ──┬──► M5 ──┐
                      │           ├──► M7 ──┼──► M9 ──► M10
                      └──► M8 ────┘           │
                             │                │
                             └──► M6 ─────────┘
M11（横切全部前端）──────────────────────────────► M12
                          │
                          └──► M13（⏸️ 现阶段排除，见 §〇）
```

**关键路径**：M0 → M1 → M2 → M3 → M4 → M9。M4（订单）延期会直接拖垮 M5/M7/M9/M10，是全局瓶颈。

> ~~→ M13~~：⏸️ 现阶段排除（见 §〇），不计入关键路径。

---

## 三、各模块详细说明

> 每个模块统一给出：**具体职责** / **应当实现的效果** / **验收标准**。
> 验收标准均为可执行、可复现的客观判据，不含「差不多」「尽量」这类模糊表述。

---

### M0 工程骨架与开发规范

**归属**：迭代一 第 1 周（A 搭前端骨架、B 搭后端骨架、D 建 Git 仓库与规范）

#### 具体职责
1. 前端脚手架：Vue3 + Vite + Element Plus + Vue Router 4 + Pinia + Axios + Sass，目录按 `src/{api,views,components,router,store,utils,layouts}` 分层。
2. 后端脚手架：Spring Boot 3.x + MyBatis-Plus + Maven 多模块或单模块分包，包名统一 `org.company.nianglin`（与团队约定一致），分层 `controller / service / mapper / entity / dto / vo / config / common`。
3. 统一响应体 `Result<T>`（code / message / data）+ 全局异常处理器 `@RestControllerAdvice`。
4. 统一日志：Logback 分级输出，脱敏规则配置文件。
5. 团队规范落地：Git 分支 `main（保护）← develop ← feature/xxx`、每周五合并；前端 ESLint + Prettier；后端 Alibaba Java 规范插件（提交前必须过）。
6. Knife4j 接入，`/doc.html` 可打开。

#### 应当实现的效果
- 新人 clone 仓库后，**两条命令**起项目：`pnpm i && pnpm dev`（前端 5173）、`mvn spring-boot:run`（后端 8080）。
- 任意一个未实现的接口调用后返回结构化错误（如 `code=501, message="功能开发中"`），而不是白页或 500 堆栈。
- 前端 `/doc.html` 反向代理或直接访问后端 `http://localhost:8080/doc.html` 能看到接口分组。

#### 验收标准
- [ ] `pnpm dev` 启动无报错、无 Vite 警告，浏览器打开 `http://localhost:5173` 出现布局骨架。
- [ ] `mvn -q clean package` 构建成功，产物可 `java -jar` 启动。
- [ ] 访问 `http://localhost:8080/doc.html` 返回 200 且能看到 Knife4j 界面。
- [ ] 故意抛异常，前端收到 `Content-Type: application/json` 且 body 符合 `Result` 结构。
- [ ] ESLint / Prettier / Alibaba 规范检查在 `git commit` 前执行，违规可拦截。
- [ ] `git branch -a` 可见 `main / develop`，`main` 已设为保护分支。

---

### M1 数据库设计与数据初始化

**归属**：迭代一 第 2–3 周（D 主导，B 参与评审）；W8 增量加打卡表/用药计划表

#### 具体职责
1. 输出 ER 图（Draw.io）、数据字典、建表 DDL 脚本。
2. 覆盖表（至少）：
   - `sys_user`（用户主表，含角色标识）
   - `elder_profile`（老人档案）
   - `family_elder_relation`（家属—老人绑定关系）
   - `companion_profile`（陪诊员资料 + 资质状态）
   - `companion_order`（陪诊订单，含状态字段 + `version` 乐观锁）
   - `order_checkin`（打卡记录，含经纬度）
   - `companion_track`（陪诊轨迹点）
   - `medicine_dict`（药品字典，仅通用信息 + 免责声明）
   - `medication_plan`（用药计划）
   - `medication_task`（每日服药任务，定时任务生成）
   - `order_review`（评价）
   - `complaint`（投诉）
   - `internal_message`（站内信）
   - `admin_oper_log`（管理员操作日志）
3. MyBatis-Plus Generator 生成 Entity / Mapper / XML / Service。
4. 种子数据：老人档案、药品字典、陪诊员数据各 **30–200 条**真实感 SQL，覆盖边界场景（空字段、超长文本、非常用药、跨天用药）。
5. 引入 Flyway 做版本化增量迁移（计划书风险 R5 应对方案）。

#### 应当实现的效果
- 一条命令（`mysql < init.sql` 或 Flyway `migrate`）从空库建出全部表并灌入种子数据。
- 所有表 `utf8mb4` + `utf8mb4_general_ci`；金额字段 `DECIMAL`；时间字段统一 `DATETIME` + 审计字段 `create_time / update_time / deleted`。
- 关键查询（订单按状态+时间分页、用药任务按用户+日期、陪诊员按资质筛选）走索引。

#### 验收标准
- [ ] 建表脚本可**重复执行**（幂等），不因已存在表而中断。
- [ ] Flyway `flyway_schema_history` 表记录版本，二次执行提示无需迁移。
- [ ] 种子数据灌入后，每张业务表 `SELECT COUNT(*)` 均 ≥ 30 条。
- [ ] 对上述 3 类关键查询执行 `EXPLAIN`，`type` 不为 `ALL`（不出现全表扫描）。
- [ ] 数据字典文档与实体类字段逐一对齐，无字段缺失、无类型不符。
- [ ] 逻辑删除字段 `deleted` 生效，删除后普通查询不可见。

---

### M2 认证与多角色鉴权

**归属**：迭代一末—迭代二初 第 3–4 周（B 主导）

#### 具体职责
1. 注册 / 登录接口；密码 **BCrypt** 加密存储。
2. JWT 签发、校验、刷新；Redis 存 token 黑名单/刷新标记。
3. Spring Security 过滤器链配置：无状态会话、放行白名单（登录/注册/静态资源/文档）。
4. 自定义注解 `@RequireRole` + `@PreAuthorize` 做**接口级**权限；角色：`ELDER / FAMILY / COMPANION / ADMIN`。
5. **老人账户默认只读**，写操作一律由家属代操作（服务层再校验一次，不只依赖前端隐藏按钮）。
6. 图形验证码（Redis 存储，5 分钟过期）+ 登录失败次数限制。
7. 手机号脱敏返回（`138****8888`）；日志脱敏，禁止打印身份证号。

#### 应当实现的效果
- 登录成功返回 token + 角色 + 菜单标识；前端按角色动态加载路由菜单。
- 越权请求被后端拦截，不依赖前端隐藏入口。
- token 过期时前端 Axios 拦截器静默刷新，用户无感知。

#### 验收标准
- [ ] 不带 token 访问受保护接口 → `401`。
- [ ] 带 FAMILY token 访问 ADMIN 接口 → `403`。
- [ ] 老人账号调用「创建订单」接口 → `403`（服务端拦截，绕过前端也无效）。
- [ ] token 过期后再次请求，前端自动刷新并重放原请求，页面不跳登录页。
- [ ] 数据库 `sys_user.password` 字段值以 `$2a$` 开头（BCrypt 特征），非明文。
- [ ] 响应体中手机号字段已脱敏，日志文件中 grep 不到完整身份证号。
- [ ] 权限测试用例覆盖 4 个角色 × 3 类接口（只读/写/管理），全部通过。

---

### M3 用户与档案管理

**归属**：W2–W5 开发，W13 补资质审核（D 加表、B 写接口）

#### 具体职责
1. 老人档案 CRUD：姓名、性别、年龄、身份证（加密/脱敏）、病史备注、紧急联系人、常去医院。
2. 家属绑定老人（邀请码 / 手机号验证），一个家属可绑定多位老人。
3. 陪诊员入驻：提交认证资料、资质证件上传、接单偏好（区域/时段）。
4. 管理员审核陪诊员资质：通过 / 驳回（附驳回原因）。
5. 用户封禁 / 解封。

#### 应当实现的效果
- 家属登录后可在「我的老人」列表代老人完成所有复杂操作。
- 陪诊员未通过审核时，陪诊员端**看不到订单大厅、不能接单**（前后端双重拦截）。
- 资质审核结果通过站内信（依赖 M8）通知本人。

#### 验收标准
- [ ] 家属可成功绑定 1 位老人，绑定后在订单页能选择该老人下单。
- [ ] 未审核陪诊员调用接单接口 → `403` 或业务码拒绝，前端订单大厅为空并给出提示。
- [ ] 审核通过后同一账号立即可以接单，无需重新登录。
- [ ] 驳回时必须填写原因，驳回后站内信可收到通知。
- [ ] 封禁用户下一次请求立即失效（不能被已签发 token 继续访问）。
- [ ] 老人档案身份证字段在数据库中为密文，接口返回为脱敏串。

---

### M4 陪诊订单与状态机

**归属**：迭代二 第 5–7 周（B 写后端、A 写前端）
**竞讲交付物（W7）**：可运行 v1.0（注册登录 + 订单创建/接单）+ 接口文档 + 测试报告

#### 具体职责
1. 下单：选择医院、科室、就诊时间、就诊人（老人）、文字地址、备注。
2. 订单 CRUD + 多条件分页查询（状态、时间范围、老人、陪诊员）。
3. **订单状态机**：`待接单 → 已接单 → 服务中 → 已完成 → 已评价`，**禁止跳级、禁止回退**；仅管理员可强制改变终态（走 M9 纠纷处理）。
4. **乐观锁 `@Version` 防陪诊员超卖接单**：并发抢同一单只能一人成功。
5. 状态变更接口必须加事务控制；每次变更写入状态流转日志。
6. 线下记账：订单完成时生成费用明细（代垫/服务费），不做在线支付。

#### 应当实现的效果
- 家属下单后订单进入「待接单」大厅；陪诊员接单后订单从大厅消失并归属该陪诊员。
- 每一个状态只能由特定角色触发，非法流转被明确拒绝并给出原因。
- 订单详情页展示完整的操作时间线。

#### 验收标准
- [ ] JMeter 50 并发同时接同一订单 → 数据库只有 1 条成功接单记录，`companion_order.version` 仅 +1。
- [ ] 直接调用「待接单 → 已完成」的跳级接口 → 返回业务错误，数据库状态不变。
- [ ] 已完成的订单再次调用「接单」→ 拒绝。
- [ ] 非订单归属陪诊员调用「服务中」→ `403`。
- [ ] 分页查询：`page=1&size=10&status=待接单` 返回条数与 `SELECT COUNT(*)` 手工核对一致。
- [ ] 状态变更接口在抛异常时事务回滚，订单状态与流转日志保持一致。
- [ ] `/doc.html` 中订单模块接口齐全，含请求/响应示例，可直接被前端 Mock 使用。

---

### M5 陪诊执行、打卡与实时进度

**归属**：迭代三 第 8–9 周（A 做打卡页与时间线、B 做接口与推送）
**竞讲交付物（W12）**：可运行 v2.0（陪诊打卡 + 用药提醒 + 评价）+ 测试报告（~~+ 用户手册~~ ⏸️ 现阶段排除，见 §〇）

#### 具体职责
1. 陪诊员打卡：GPS 定位（**模拟坐标**，一期不做地图导航）+ 打卡类型（出发 / 到院 / 就诊中 / 取药 / 离院 / 完成）+ 现场照片（可选）。
2. 打卡落库 `order_checkin`，同时写入 `companion_track` 轨迹点。
3. 家属端「陪诊进度」页：状态时间线，实时刷新。
4. **WebSocket 或 SSE** 把进度事件推给家属端，前端用 `ElNotification` 弹提示。
5. 异常与断线处理：连接断开自动重连，重连后补齐未读事件。

#### 应当实现的效果
- 陪诊员点「到院打卡」，家属端 3 秒内自动出现一条进度记录并弹出通知，**无需手动刷新页面**。
- 一次陪诊结束后，时间线完整可回放（几点出发、几点到院、几点取药、几点完成）。
- 打卡坐标落在订单地址合理范围内才算有效（距离阈值校验，防虚假打卡）。

#### 验收标准
- [ ] 打卡接口落库成功，`order_checkin` 与 `companion_track` 同时新增记录且订单 ID 一致。
- [ ] 家属端在不刷新页面的前提下，3 秒内收到陪诊进度推送。
- [ ] 关闭网络 30 秒再恢复，WebSocket/SSE 自动重连，重连后不丢事件、不重复渲染。
- [ ] 打卡坐标超出阈值 → 返回业务错误，记录不写入或标记为「异常打卡」。
- [ ] 时间线顺序与数据库 `order_checkin.create_time` 升序完全一致。
- [ ] 同一陪诊员对同一订单重复提交同一打卡类型 → 去重，不产生重复记录。

---

### M6 用药管理与漏服提醒

**归属**：迭代三 第 9–10 周（D 加表、B 写定时任务、A 写日历页）
**合规红线**：只做「记录与提醒」，不得出现任何诊断结论或用药建议

#### 具体职责
1. 药品字典维护：药品名、规格、通用适应症说明、注意事项、**免责声明**（不提供剂量建议）。
2. 用药计划 CRUD：药品、剂量、频次（每日 N 次）、起止日期、饭前/饭后。
3. 服药日历页：按月/周视图展示服药记录与状态（待服 / 已服 / 漏服）。
4. **Spring Task 定时任务**：每天 07:00 生成当日 `medication_task`。
5. **每 30 分钟扫描漏服**并向家属推送（走 M8 站内信 / 推送）。
6. **Redis 分布式锁**保证定时任务在多实例下只执行一次，不重复生成。
7. 服药确认：家属或陪诊员标记「已服」，记录操作人。

#### 应当实现的效果
- 每天早上 7 点后打开日历，当天所有待服任务已自动生成，无需人工点击。
- 超过设定服药时间窗口未确认 → 状态自动变为「漏服」并推送给家属。
- 答辩时可展示「漏服率对比」数据（技术亮点中的量化指标）—— ⏸️ 现阶段不做 PPT，演示通过本机 `mvn spring-boot:run` + `/doc.html` 兜底（见 §〇）。

#### 验收标准
- [ ] 手动触发定时任务两次，`medication_task` 不出现重复行（唯一索引 + 分布式锁双重保障）。
- [ ] 把某条任务时间设置为过去时间，下一次扫描后状态自动变为「漏服」且推送到位。
- [ ] 日历页显示结果与 `SELECT * FROM medication_task WHERE user_id=? AND date=?` 逐条一致。
- [ ] 并发启动 2 个后端实例，定时任务仅 1 个实例实际执行（日志可证）。
- [ ] 全站搜索「建议服用」「推荐剂量」等词，结果为空；药品详情页存在免责声明。
- [ ] 服药确认按钮的记录中包含操作人与操作时间。

---

### M7 评价与投诉

**归属**：迭代三 第 10 周（A 写页面、B 写接口）

#### 具体职责
1. 订单完成后的评价：1–5 星评分 + 标签（准时/耐心/专业…）+ 文字 + 匿名开关。
2. 陪诊员评分聚合统计（平均分、评价数、星级分布）。
3. 投诉提交（关联订单）与处理流转。
4. 评价与投诉的敏感词过滤。

#### 应当实现的效果
- 只有「已完成」状态的订单才出现「去评价」入口。
- 陪诊员详情页显示综合评分与最近评价列表。
- 投诉可被管理员受理、处理、结案，全过程留痕。

#### 验收标准
- [ ] 「待接单 / 已接单 / 服务中」状态订单调用评价接口 → 被拒绝。
- [ ] 同一订单重复评价 → 被拒绝（唯一约束）。
- [ ] 陪诊员平均分 = 该陪诊员所有有效评价的平均值（与 SQL `AVG` 手工核对一致）。
- [ ] 投诉提交后管理后台可见，状态可流转为「处理中 → 已结案」，每步记录操作人与时间。
- [ ] 命中敏感词的评价/投诉标题无法提交。

---

### M8 站内信与通知

**归属**：W9–W12（B 主导）
**背景**：一期砍掉 IM 聊天，用站内信替代（计划书范围控制）

#### 具体职责
1. 站内信表与接口：列表（分页）、详情、标记已读、全部已读、未读数。
2. 五类消息模板：订单事件、资质审核结果、漏服提醒、**投诉事件**、系统公告。
3. 通过 **SSE**（`GET /sse/message`，事件名 `NEW_MESSAGE`）实时推送未读数变化，前端顶栏红点提示。
   > 实现口径以 `docs/api/07-message.md` §6 为准；一期前端 `EventSource` 无法带 `Authorization` 头，
   > 故实际走 60 秒轮询兜底，SSE 作为秒级通道预留。
4. 消息已读状态持久化，多端一致。

#### 应当实现的效果
- 订单被接单 / 打卡进度 / 审核通过 / 漏服提醒 / **新投诉**，均自动生成站内信。
- 顶栏铃铛实时显示未读数，点击列表可逐条/一键已读。

#### 验收标准
- [ ] 触发订单接单事件后，家属账号站内信列表新增对应消息，未读数 +1。
- [ ] 家属提交投诉后，全部管理员与被投诉方各收到一条 `COMPLAINT_SUBMITTED` 站内信。
- [ ] 点击消息后未读数 -1，刷新页面状态不复原（已持久化）。
- [ ] 未读数变化在 3 秒内推送到前端，无需刷新。
- [ ] 分页接口在 100+ 条数据下返回正确页数，边界页（首页/末页/超范围页）不出错。
- [ ] 站内信内容不包含身份证号等敏感明文。

---

### M9 管理后台

**归属**：迭代四 第 13 周启动，迭代五 第 14 周完成（A 做页面、B 做接口、C 做测试、D 加操作日志表）
**竞讲交付物（W13）**：评审改进清单 + 管理后台 v1

#### 具体职责
1. 陪诊员资质审核（通过 / 驳回 + 原因）。
2. 用户管理：查询、封禁、解封、重置密码。
3. 订单管理：查看全部订单、按条件筛选、**纠纷处理**（强制改终态 + 备注）。
4. 管理员操作日志：所有管理动作写 `admin_oper_log`（谁、何时、对谁、做了什么）。
5. 管理端权限与业务端隔离（ADMIN 角色专属路由 + 接口）。

#### 应当实现的效果
- 管理员可以在一个后台完成「审资质 → 管用户 → 管订单 → 处理纠纷」全流程。
- 每一次管理操作都可追溯，日志页可按操作人 / 时间 / 类型检索。

#### 验收标准
- [ ] 审核列表能正确显示待审核 / 已通过 / 已驳回三种状态及数量。
- [ ] 封禁用户后，该用户任意接口请求立即返回 403。
- [ ] 每次审核/封禁/纠纷处理，`admin_oper_log` 都新增一条记录（含操作人 ID、目标 ID、动作、时间）。
- [ ] 纠纷处理可把订单从「服务中」强制置为「已完成」或「已取消」，并给双方各发一条站内信。
- [ ] 非 ADMIN 角色访问管理端接口 → `403`，且前端路由守卫拦在页面之外。
- [ ] 日志页支持按时间区间 + 操作类型筛选，结果正确。

---

### M10 数据统计、可视化与导出

**归属**：迭代五 第 14–15 周（A 做 ECharts 看板、B 做统计与导出接口）

#### 具体职责
1. 统计接口：订单量、订单完成率、用户增长（日/周/月维度）、陪诊员接单排行、漏服率。
2. ECharts 可视化看板：柱状图、饼图、折线图，支持时间区间切换。
3. **EasyExcel** 导出订单数据、用户数据为 Excel（按当前筛选条件导出）。
4. 统计查询走索引优化（计划书 W11「优化慢查询」）。

#### 应当实现的效果
- 管理后台首页打开即为数据看板，图表随筛选条件联动刷新。
- 导出的 Excel 可直接用 Office 打开，列头中文、数据与页面一致。

#### 验收标准
- [ ] 看板中「订单总量」与 `SELECT COUNT(*) FROM companion_order` 手工核对一致。
- [ ] 「完成率」= 已完成订单数 / 总订单数，与 SQL 计算值一致（保留 2 位小数）。
- [ ] 切换时间区间后图表数据随之变化，且区间外数据不混入。
- [ ] 导出的 `.xlsx` 文件可正常打开，行数 = 当前筛选条件下的记录数，无乱码。
- [ ] 统计接口在 1 万条订单数据下响应时间 < 2s。
- [ ] 无数据时图表显示空状态而非报错或空白。

---

### M11 适老化前端体系

**归属**：横切 W3–W16（A 主导，C 负责 UI 走查）

#### 具体职责
1. 全局主题：`el-config-provider` 统一尺寸/字号切换；高对比度配色；大圆角、大间距、大触控区。
2. **老人模式**：一键切换（~~答辩现场演示用~~ ⏸️ 现阶段不做 PPT，见 §〇），使用一套简化路由 + 极简操作，隐藏次要功能。
3. 老人端只读：所有复杂操作引导至「请让家人帮您操作」。
4. 按钮最小高度 ≥ 44px；正文最小字号 ≥ 18px；颜色对比度 ≥ 4.5:1。
5. 关键流程（挂号陪诊下单）步数 ≤ 3 步。

#### 应当实现的效果
- 顶栏一个开关，点击后整个站点字号放大、配色加深、菜单精简，**无需刷新页面**。
- 任一页面在老人模式下文字仍不溢出、不重叠、不发生截断。

#### 验收标准
- [ ] 点击「老人模式」开关，全局字号与配色立即变化，浏览器控制台无报错。
- [ ] 老人模式下正文计算字号 ≥ 18px（DevTools 实测）。
- [ ] 用对比度检测工具抽查 5 处主要文字，对比度均 ≥ 4.5:1。
- [ ] 1366×768 与 1920×1080 两种分辨率下，无横向滚动条、无元素重叠。
- [ ] 老人端核心流程点击次数 ≤ 3 次。
- [ ] 切换模式后刷新页面，模式偏好被记住（localStorage / Pinia 持久化）。

---

### M12 测试与质量保障

**归属**：横切 W11（编写）、W7/W12/W16（测试报告交付）— C 主导、B 配合

#### 具体职责
1. JUnit5 + Mockito 单元测试（Service 层为主）。
2. MockMvc 接口测试 + **多角色越权用例**（覆盖 4 角色 × 只读/写/管理）。
3. JMeter 压测抢单接口（并发接单、接口响应时间、错误率），输出对比表格。
4. 功能测试用例文档 + Bug 清单 + 回归测试。
5. 测试报告归档（迭代二 / 三 / 五各一份）。

#### 应当实现的效果
- 一条命令跑完全部单测并输出覆盖率报告。
- 越权场景 100% 覆盖，不存在「换个 token 就能操作别人数据」的漏洞。
- 压测报告能直观说明乐观锁对超卖的控制效果（作为工作量证明）。

#### 验收标准
- [ ] `mvn test` 全绿，无跳过用例。
- [ ] 核心 Service 层单元测试覆盖率 ≥ 60%（JaCoCo 报告为证）。
- [ ] 越权用例：4 角色 × 3 类接口 = 12 条，全部通过。
- [ ] JMeter 压测：同一订单 50 并发，成功数 = 1，失败数为 49 且错误信息明确。
- [ ] 每个迭代的测试报告包含：用例数、通过数、失败数、遗留 Bug 及处理结论。
- [ ] 遗留 Bug 清单按严重级别排序，P0/P1 必须清零才能提交竞讲。

---

### M13 部署与交付物

> ⏸️ **本模块现阶段主动排除（2026-09-16 决策）**，不作为本阶段交付承诺。
> 详见 §〇 范围调整；以下原文仅作历史归档保留，便于以后重启时取用。

**归属**：迭代五 第 15 周、迭代六 第 17 周（D 主导、C 配合）
**~~竞讲交付物（W16）~~**：v3.0 完整系统 + 完整测试报告 + ~~用户手册~~ + ~~部署文档~~
**~~最终交付物（W17）~~**：最终版系统 + 全部文档 + ~~答辩 PPT~~ + ~~演示视频~~

#### 具体职责
1. ~~`Dockerfile` + `docker-compose.yml`：一键起 MySQL / Redis / 后端应用；`deploy.sh` 一键脚本。~~
2. ~~前端构建产物部署（Nginx 反代 + 静态托管）。~~
3. ~~部署文档：环境要求、启动步骤、常见问题。~~
4. ~~用户手册：按角色分章节（老人 / 家属 / 陪诊员 / 管理员），含操作截图。~~
5. ~~演示视频（每个迭代一段）、答辩 PPT（每迭代一份）。~~
6. ~~全链路演练：竞讲前 24 小时演练（风险 R6 应对）。~~

#### 应当实现的效果
- ~~在一台**全新机器**上，按部署文档 3 步内拉起完整系统。~~
- ~~演示环境不依赖手工灌数据（风险 R8 应对），种子脚本覆盖多种边界场景。~~

#### 验收标准
- [ ] ~~`deploy.sh` 可重复执行且幂等，第二次执行不报错。~~
- [ ] ~~`docker compose up -d` 后，`docker compose ps` 三个服务状态均为 `healthy` / `running`。~~
- [ ] ~~全新机器部署后，浏览器访问前端首页、后端 `/doc.html` 均正常。~~
- [ ] ~~用户手册中每个角色章节至少 5 张带标注的操作截图。~~
- [ ] ~~交付物清单（7 项）全部上传教学平台，文件名与计划书一致。~~
- [ ] ~~竞讲前 24 小时完成一次全链路演练并记录结果。~~

> ✅ **替代兜底**：本阶段专注代码编写，演示通过本机 `mvn spring-boot:run` + Knife4j `/doc.html` 完成；
> 待 W17 启动答辩准备时，再按本章节原文恢复。

---

## 四、当前本地开发环境检测结果

> 检测时间：2026-09-15　检测方式：PowerShell 服务/注册表/命令探测 + 端口扫描

### 4.1 主机

| 项 | 检测值 |
|---|---|
| 操作系统 | Microsoft Windows 11 专业版（Build 26100，64 位） |
| CPU | AMD Ryzen 7 8745H（8 核 16 线程） |
| 内存 | 31.3 GB |
| 磁盘可用 | C: 126.5 GB / D: 503.8 GB / F: 734 GB |

### 4.2 已具备的运行时与工具链

| 组件 | 项目要求 | 本机实际 | 判定 |
|---|---|---|---|
| JDK | 17+（Spring Boot 3.x） | **21.0.7 LTS**，`JAVA_HOME=D:\develop\Java\jdk-21`；另装 1.8 / 11.0.2 / 17 / 25 | ✅ 用 21 |
| Maven | 3.6+ | **3.9.15**（`F:\software\apache-maven-3.9.15`，已在 PATH） | ✅ |
| Gradle | 可选 | 8.14.4 | ✅ 用不上 |
| Node.js | 18+（Vite 5）/ 20.19+（Vite 7） | **22.22.2** | ✅ |
| npm | 9+ | **10.9.7**，registry 已指向 `registry.npmmirror.com` | ✅ |
| pnpm | 可选 | **12.4.1**（全局） | ✅ |
| Git | 任意 | **2.55.0** + TortoiseGit 2.18 | ✅ |
| MySQL | 8.0 | **8.0.42**，服务 `MySQL80` 运行中，监听 3306 | ✅ |
| MySQL 客户端 | — | `C:\Program Files\MySQL\MySQL Server 8.0\bin\mysql.exe` | ✅ |
| Redis | 5+ | **redis-windows 8.8.0**（`D:\develop\Redis-8.8.0`），服务 `Redis` 运行中，监听 6379 | ✅ 见风险 4.4-② |
| Redis 客户端 | — | `redis-cli.exe` 已在 PATH；另有 Another Redis Desktop Manager 1.7.4 | ✅ |
| 数据库 GUI | Navicat | 无 Navicat，但有 **DataGrip**（`D:\develop\DataGrip`） | ✅ 可替代 |
| IDE | IDEA | **IntelliJ IDEA 2026.1.1** | ✅ |
| 编辑器 | — | VS Code（`D:\develop\Microsoft VS Code`） | ✅ |
| 浏览器 | Chrome/Edge | Edge 已内置；**无 Chrome** | ⚠️ 见 4.3-⑥ |
| ~~Nginx~~ | ~~可选（部署用）~~ | nginx 1.28.3 已装但**现阶段不用** | ⏸️ 见 §〇 |
| Office | 文档产出 | Microsoft Office 2021 专业增强版 | ✅ |
| Python | 辅助脚本 | 3.12.7 + Miniconda 3.14.7 | ✅ |
| ~~Docker~~ | ~~一键部署~~ | ⏸️ 现阶段排除（见 §〇），无需安装 | — |
| JMeter | 压测 | **未安装** | ❌ 见 4.3-③ |
| PlantUML / StarUML / draw.io | 画图 | **均未安装** | ❌ 见 4.3-④ |

端口占用情况：3306（MySQL）、33060（MySQL X）、6379（Redis）已被本机服务占用 —— 即项目依赖的中间件**已在运行**；8080 / 5173 空闲，可直接用于前后端。

### 4.3 需要补装 / 补配的组件清单

按优先级排序（P0 = 不解决就跑不起来；P1 = 影响某模块交付；P2 = 提升效率；P3 = 可选）。

| # | 优先级 | 组件 | 版本要求 | 用途 / 对应模块 | 安装方式 |
|---|---|---|---|---|---|
| ① | **P0** | **Maven 镜像配置 `~/.m2/settings.xml`** | 阿里云 / 腾讯云公共镜像 | M0 起全部模块。本机 `C:\Users\wang\.m2\settings.xml` **不存在**，首次拉取 Spring Boot 3.x 依赖会极慢甚至超时 | 新建 `settings.xml`，配置 `<mirror>` 指向 `https://maven.aliyun.com/repository/public` |
| ② | **P1** | **Apache JMeter** | **5.6.3**（需 JDK 8+，本机 21 可用） | M12 压测抢单接口（计划书明确要求「出对比表格即工作量证明」） | 下载 `apache-jmeter-5.6.3.zip`，解压到 `D:\develop\jmeter`，配 `JMETER_HOME` |
| ③ | **P1** | **PlantUML** + **draw.io Desktop** | PlantUML 1.2024+；draw.io 24.x | M1 ER 图 / 用例图 / 架构图 / 活动图（计划书「技术准备」列明） | PlantUML 用 IDEA 插件 + Graphviz（需单独装 Graphviz 12.x）；draw.io 用桌面版 exe |
| ④ | ~~P1~~ | ~~Docker Desktop + docker-compose v2~~ | — | ⏸️ M13 一键部署现阶段排除（见 §〇），**本阶段不需要安装** | — |
| ⑤ | **P2** | **Google Chrome** | 最新稳定版 | M11 跨浏览器兼容测试（目前仅有 Edge） | 官网下载；或用 Edge 代替并说明 |
| ⑥ | **P2** | **Navicat for MySQL**（可选） | 16.x | 数据库日常查看（计划书「技术准备」提到 Navicat）。**已有 DataGrip 可完全替代，非必需** | 官方下载，或用现有 DataGrip |
| ⑦ | ~~P3~~ | ~~云服务器（可选）~~ | — | ⏸️ 现阶段不做部署上线（见 §〇） | — |

### 4.4 环境风险与注意事项

1. ~~**Docker 是本机唯一的硬缺口。**~~
   ~~计划书两处依赖它（技术亮点第 10 条一键部署、风险 R6 竞讲环境兜底）。若安装受阻（WSL2 需管理员权限 + 重启 + BIOS 虚拟化开启），退路方案：~~
   - ~~保留 `Dockerfile` / `docker-compose.yml` / `deploy.sh` 文件照写（答辩展示用），实际演示改用「本机 MySQL 8.0 + Redis + `java -jar` + Nginx 托管前端」的方式启动；~~
   - ~~竞讲时用录屏视频兜底（风险 R6 已有此预案）。~~
   - ⏸️ **2026-09-16 决策**：M13 整体排除，本缺口不再成立；演示通过本机 `mvn spring-boot:run` + Knife4j `/doc.html` 兜底（见 §〇）。

2. **本机 Redis 是 Windows 移植版（redis-windows 8.8.0），不是官方 Windows 支持版本。**
   Windows 下 Redis 无法真正 fork，RDB 持久化与部分高版本特性受限。对课程项目足够，但：
   - 分布式锁请用 `SET key value NX PX 30000` + Lua 脚本释放，不要依赖 Redisson 的高级特性或 Redis 7 Functions；
   - 不要把 Redis 当作唯一数据存储，所有持久化数据必须落 MySQL。

3. **Maven 无镜像配置是隐形坑。**
   `C:\Users\wang\.m2\settings.xml` 不存在，说明本机从未配过私服/镜像。Spring Boot 3.x + MyBatis-Plus + Knife4j + EasyExcel 全套依赖首次下载量很大，务必在 M0 阶段先配好阿里云镜像。

4. **JDK 版本管理。**
   本机装有 1.8 / 11.0.2 / 17 / 21 / 25 五个 JDK。`JAVA_HOME` 当前指向 21，符合项目要求。**注意**：IDEA 项目 SDK 与 Maven `pom.xml` 的 `<java.version>` 必须显式设为 21，避免 IDE 误用 25 或 8 导致编译失败。

5. **MySQL root 非默认密码。**
   探测时 `root/root` 认证失败，说明已设自定义密码。M0 阶段需要确认密码并写入 `application-dev.yml`，**不要提交到 Git**（用环境变量或 `application-local.yml` + `.gitignore`）。

6. **本次检测中发现：本机 Git Bash 环境的 `PATH` 存在异常**（`ls` / `cat` / `head` / `uname` / `find` 等基础命令不可用）。
   若在 Git Bash 里执行脚本或跑 Maven 出现 `command not found`，改用 PowerShell 或 CMD。这不影响 IDEA / VS Code 内的开发流程。

7. **端口规划（建议，需在 M0 落地到配置）**

   | 用途 | 端口 | 状态 |
   |---|---|---|
   | MySQL | 3306 | 已运行 |
   | Redis | 6379 | 已运行 |
   | 后端 Spring Boot | 8080 | 空闲 |
   | 前端 Vite dev | 5173 | 空闲 |
   | Knife4j 文档 | 8080/doc.html | — |
   | ~~前端生产预览（Nginx）~~ | ~~80~~ | ⏸️ 见 §〇，本阶段不做 |

4.5 项目内依赖（随 `pom.xml` / `package.json` 引入，无需单独安装）

**后端（Maven）**

| 依赖 | 建议版本 | 对应模块 |
|---|---|---|
| `spring-boot-starter-parent` | 3.3.x 及以上（配 JDK 21） | 全部 |
| `spring-boot-starter-web` / `validation` / `aop` | 随 parent | M0, M2 |
| `spring-boot-starter-security` | 随 parent | M2 |
| `spring-boot-starter-data-redis` | 随 parent | M2, M6 |
| `spring-boot-starter-websocket` | 随 parent | M5, M8 |
| `mybatis-plus-spring-boot3-starter` | 3.5.7+ | M1, M4 |
| `mysql-connector-j` | 8.0.33+ / 8.4.x | M1 |
| `jjwt-api` / `jjwt-impl` / `jjwt-jackson` | 0.12.x | M2 |
| `knife4j-openapi3-jakarta-spring-boot-starter` | 4.5.0 | M0 |
| `easyexcel` | 3.3.4+ | M10 |
| `flyway-core` + `flyway-mysql` | 与 Spring Boot 3.3 匹配（Flyway 10.x） | M1 |
| `lombok` | 1.18.30+ | 全部 |
| `hutool-all` | 5.8.x | 工具类 |
| `spring-boot-starter-test` + `mockito` | 随 parent | M12 |

**前端（npm / pnpm）**

| 依赖 | 建议版本 | 对应模块 |
|---|---|---|
| `vue` | 3.4+ | 全部 |
| `vite` | 5.x 或 6.x（Node 22 满足） | M0 |
| `element-plus` | 2.8+ | 全部 |
| `vue-router` | 4.x | M0, M2 |
| `pinia` | 2.x | M0, M2 |
| `axios` | 1.x | M0, M2 |
| `echarts` | 5.5+ | M10 |
| `sass` / `sass-embedded` | 最新（本机已有全局 sass-embedded 1.104.0，仍需装项目内依赖） | M0, M11 |
| `eslint` + `prettier` | 最新 | M0 |
| `unplugin-auto-import` / `unplugin-vue-components` | 最新（可选，按需引入 Element Plus） | M0 |

---

## 五、实施里程碑对照（来自计划书，供排期参考）

| 迭代 | 周次 | 主题 | 竞讲交付物 | 涉及模块 |
|---|---|---|---|---|
| 一 | W1–W4 | 需求分析与系统架构 | 需求规格说明书 + 用例图 + 系统架构图 + ER 图 + 原型图 | M0, M1, M2, M11 |
| 二 | W5–W7 | 用户与订单模块 | v1.0 系统 + 接口文档 + 测试报告（~~+ 演示视频~~ ⏸️ 见 §〇） | M3, M4, M8 |
| 三 | W8–W12 | 陪诊执行与用药管理 | v2.0 系统 + 测试报告（~~+ 用户手册 + 演示视频~~ ⏸️ 见 §〇） | M5, M6, M7, M8, M11 |
| 四 | W13 | 中期评审改进 + 管理后台 | 评审改进清单 + 管理后台 v1 | M9 |
| 五 | W14–W16 | 管理后台 + 数据统计 + 系统完善 | v3.0 完整系统 + 完整测试报告（~~+ 用户手册 + 部署文档~~ ⏸️ 见 §〇） | M9, M10（M13 ⏸️ 排除） |
| 六 | W17 | 最终答辩准备 | 最终版系统 + 全部代码 + ~~答辩 PPT + 演示视频~~（⏸️ 见 §〇） | 代码整理 |

### W4 竞讲前必须完成的最小闭环

```
M0 骨架跑起来  →  M1 建表 + 种子数据  →  M2 登录返回 JWT  →  M11 布局骨架可看
```

---

## 六、风险对照（计划书原文 + 模块级应对）

| 编号 | 风险 | 等级 | 关联模块 | 应对（落实到模块） |
|---|---|---|---|---|
| R1 | 选题范围过大做不完 | 高 | 全部 | 严守一期范围：砍掉在线支付/地图导航/IM；M13 中把三项写入「二期规划」 |
| R2 | 成员进度不均衡 | 高 | 全部 | 每晚 22:00 站会；每周五合并；落后成员转文档/测试 |
| R3 | 前后端接口字段不一致 | 中 | M0, M4 | M0 阶段强制 Knife4j 先出接口文档 + Mock 数据；接口变更必须群内提前通知 |
| R4 | JWT 多角色越权漏洞 | 中 | M2, M12 | 统一 `@PreAuthorize` / `@RequireRole`；M12 编写 12 条越权用例 |
| R5 | 数据库设计不合理后期大量改表 | 中 | M1, M6 | 迭代一充分讨论 ER 图；M1 引入 Flyway 支持增量迁移 |
| R6 | ~~竞讲前演示环境不稳定~~ | — | — | ⏸️ M13 排除（见 §〇），演示通过本机 `mvn spring-boot:run` + `/doc.html` 兜底；风险不再成立 |
| R7 | 适老化 UI 被质疑「太简单」 | 低 | M11 | 用 `el-config-provider` 全局尺寸切换 + 简化路由佐证技术实现；引用国家适老化改造政策 |
| R8 | 数据造假被问穿 | 低 | M1, M13 | 老人档案/药品字典/陪诊员数据各备 30–200 条真实感 SQL，演示不手输 |

---

## 七、下一步动作清单（按优先级）

1. **【立即 · P0】** 创建 `C:\Users\wang\.m2\settings.xml`，配置阿里云镜像 — 不然后面每一步都要等依赖下载。
2. **【立即 · P0】** 确认 MySQL root 密码，建库 `nianglin`（`utf8mb4_general_ci`）。
3. **【W1】** 执行 M0：前后端脚手架 + 统一响应/异常 + Knife4j + Git 分支规范。
4. ~~**【W1–W2】** 决策 Docker 是否安装（P1）；同步启动 M1 的 ER 图与建表脚本。~~ — ⏸️ 现阶段排除（见 §〇），本决策无需做；ER 图与建表脚本同步推进。
5. **【W2】** 补装 PlantUML/Graphviz 与 draw.io（M1 画图需要）。
6. **【W11 前】** 补装 JMeter 5.6.3（M12 需要）。
7. ~~**【W15 前】** 补装 Docker Desktop（M13 需要）；若受阻，走退路方案。~~ — ⏸️ 现阶段排除（见 §〇），本动作不执行。

---

*本文档为项目开发的可执行基线，后续每次迭代评审后应更新对应模块的验收项与周次。*
