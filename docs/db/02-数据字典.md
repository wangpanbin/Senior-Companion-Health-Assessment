# 银龄伴诊 · 数据字典（M1）

> 逐表逐字段说明。**可空**列标注 `Y`/`N`，`—` 表示无默认值。

全域图例：`PK` 主键 · `UK` 唯一 · `I` 已建索引

## 一、账号与权限域

### 1. `sys_user` — 用户主表（四类角色共用）

- 模块：`M2/M3`　种子数据：**92** 行

| 字段 | 类型 | 可空 | 默认值 | 说明 |
|---|---|---|---|---|
| `id` `PK` | `BIGINT` | N | 自增 | 用户 ID |
| `username` `UK` | `VARCHAR(50)` | N | — | 登录用户名，4-20 位字母数字下划线 |
| `password` | `VARCHAR(100)` | N | — | 登录密码，BCrypt 哈希（$2a$ 开头），禁止明文/可逆加密 |
| `nickname` | `VARCHAR(50)` | N | — | 昵称（如「张大爷」「李阿姨」），面向老人的称呼 |
| `real_name` | `VARCHAR(50)` | Y | NULL | 真实姓名，陪诊员/管理员使用，老人与家属可空 |
| `phone` `UK` | `VARCHAR(20)` | N | — | 手机号，唯一，接口返回时须脱敏 |
| `avatar` | `VARCHAR(255)` | Y | NULL | 头像 URL |
| `role` | `VARCHAR(20)` | N | — | 角色：ELDER-老年患者 / FAMILY-家属 / COMPANION-陪诊员 / ADMIN-管理员 |
| `status` | `VARCHAR(20)` | N | 'NORMAL' | 账号状态：NORMAL-正常 / DISABLED-已封禁 |
| `need_change_password` | `TINYINT` | N | 0 | 是否需强制改密：0-否 1-是（管理员重置密码后置 1） |
| `last_login_time` | `DATETIME` | Y | NULL | 最后登录时间 |
| `last_login_ip` | `VARCHAR(50)` | Y | NULL | 最后登录 IP |
| `remark` | `VARCHAR(255)` | Y | NULL | 备注（封禁原因等） |
| `create_time` | `DATETIME` | N | CURRENT_TIMESTAMP | 创建时间 |
| `update_time` | `DATETIME` | N | CURRENT_TIMESTAMP | 更新时间 |
| `deleted` | `TINYINT` | N | 0 | 逻辑删除：0-未删除 1-已删除 |

**索引**

| 类型 | 名称 | 列 | 说明 |
|---|---|---|---|
| 主键 | `PRIMARY` | `id` | 自增主键 |
| 唯一 | `uk_username` | `username` |  |
| 唯一 | `uk_phone` | `phone` |  |
| 普通 | `idx_role_status` | `role`, `status` |  |
| 普通 | `idx_create_time` | `create_time` |  |
| 普通 | `idx_nickname` | `nickname` |  |

### 2. `sys_login_log` — 登录日志表（只增不改）

- 模块：`M2`　种子数据：**40** 行

| 字段 | 类型 | 可空 | 默认值 | 说明 |
|---|---|---|---|---|
| `id` `PK` | `BIGINT` | N | 自增 | 日志 ID |
| `user_id` | `BIGINT` | Y | NULL | 用户 ID，账号不存在时为 NULL |
| `username` | `VARCHAR(50)` | Y | NULL | 登录时输入的用户名 |
| `login_type` | `VARCHAR(20)` | Y | NULL | 类型：LOGIN-登录 / LOGOUT-登出 / REFRESH-刷新令牌 |
| `status` | `VARCHAR(20)` | N | — | 结果：SUCCESS-成功 / FAIL-失败 |
| `fail_reason` | `VARCHAR(200)` | Y | NULL | 失败原因（账号不存在/密码错误/验证码错误/账号封禁/锁定） |
| `ip` | `VARCHAR(50)` | Y | NULL | 客户端 IP |
| `user_agent` | `VARCHAR(255)` | Y | NULL | 浏览器 UA |
| `login_time` | `DATETIME` | N | — | 发生时间 |
| `create_time` | `DATETIME` | N | CURRENT_TIMESTAMP | 创建时间 |
| `update_time` | `DATETIME` | N | CURRENT_TIMESTAMP | 更新时间 |
| `deleted` | `TINYINT` | N | 0 | 逻辑删除：0-未删除 1-已删除 |

**索引**

| 类型 | 名称 | 列 | 说明 |
|---|---|---|---|
| 主键 | `PRIMARY` | `id` | 自增主键 |
| 普通 | `idx_user_time` | `user_id`, `login_time` |  |
| 普通 | `idx_username` | `username` |  |
| 普通 | `idx_status_time` | `status`, `login_time` |  |

### 3. `sys_dict` — 数据字典表

- 模块：`M0`　种子数据：**45** 行

| 字段 | 类型 | 可空 | 默认值 | 说明 |
|---|---|---|---|---|
| `id` `PK` | `BIGINT` | N | 自增 | 字典项 ID |
| `dict_type` | `VARCHAR(50)` | N | — | 字典类型：HOSPITAL-医院 / DEPARTMENT-科室 / SERVICE_TYPE-陪诊服务类型 / COMPLAINT_TYPE-投诉类型 |
| `dict_code` | `VARCHAR(50)` | N | — | 字典编码（类型内唯一） |
| `dict_label` | `VARCHAR(100)` | N | — | 显示文本 |
| `dict_value` | `VARCHAR(100)` | Y | NULL | 附加值（如排序权重、附加说明） |
| `sort_no` | `INT` | N | 0 | 排序号，越小越靠前 |
| `status` | `TINYINT` | N | 1 | 状态：0-停用 1-启用 |
| `remark` | `VARCHAR(255)` | Y | NULL | 备注 |
| `create_time` | `DATETIME` | N | CURRENT_TIMESTAMP | 创建时间 |
| `update_time` | `DATETIME` | N | CURRENT_TIMESTAMP | 更新时间 |
| `deleted` | `TINYINT` | N | 0 | 逻辑删除：0-未删除 1-已删除 |

**索引**

| 类型 | 名称 | 列 | 说明 |
|---|---|---|---|
| 主键 | `PRIMARY` | `id` | 自增主键 |
| 唯一 | `uk_type_code` | `dict_type`, `dict_code` |  |
| 普通 | `idx_type_sort` | `dict_type`, `sort_no` |  |

### 4. `sys_file` — 附件统一登记表

- 模块：`M0/M3`　种子数据：**36** 行

| 字段 | 类型 | 可空 | 默认值 | 说明 |
|---|---|---|---|---|
| `id` `PK` | `BIGINT` | N | 自增 | 记录 ID |
| `file_id` `UK` | `VARCHAR(64)` | N | — | 对外暴露的文件标识（上传接口返回的 fileId） |
| `origin_name` | `VARCHAR(255)` | Y | NULL | 原始文件名（仅记录，不用于落盘路径） |
| `stored_name` | `VARCHAR(255)` | N | — | 磁盘存储文件名（UUID 重命名，防路径穿越） |
| `url` | `VARCHAR(255)` | N | — | 可访问 URL，如 /uploads/202609/xxx.jpg |
| `store_path` | `VARCHAR(255)` | Y | NULL | 磁盘相对路径 ./uploads/{yyyyMM}/{uuid}.{ext} |
| `size` | `BIGINT` | Y | NULL | 字节数 |
| `ext` | `VARCHAR(20)` | Y | NULL | 扩展名：jpg / png / webp / pdf |
| `content_type` | `VARCHAR(100)` | Y | NULL | 服务端探测到的真实 MIME（按文件头判定，不信任前端） |
| `biz_type` | `VARCHAR(20)` | Y | NULL | 业务类型：COMPANION_CERT-资质证件 / CHECKIN-打卡照片 / COMPLAINT-投诉证据 / AVATAR-头像 / REVIEW-评价图片 |
| `biz_id` | `BIGINT` | Y | NULL | 业务主键 ID |
| `uploader_id` | `BIGINT` | Y | NULL | 上传人用户 ID |
| `create_time` | `DATETIME` | N | CURRENT_TIMESTAMP | 创建时间 |
| `update_time` | `DATETIME` | N | CURRENT_TIMESTAMP | 更新时间 |
| `deleted` | `TINYINT` | N | 0 | 逻辑删除：0-未删除 1-已删除 |

**索引**

| 类型 | 名称 | 列 | 说明 |
|---|---|---|---|
| 主键 | `PRIMARY` | `id` | 自增主键 |
| 唯一 | `uk_file_id` | `file_id` |  |
| 普通 | `idx_biz` | `biz_type`, `biz_id` |  |
| 普通 | `idx_uploader` | `uploader_id` |  |

### 5. `admin_oper_log` — 管理员操作日志表（只增不改不删）

- 模块：`M9`　种子数据：**37** 行

| 字段 | 类型 | 可空 | 默认值 | 说明 |
|---|---|---|---|---|
| `id` `PK` | `BIGINT` | N | 自增 | 日志 ID |
| `operator_id` | `BIGINT` | N | — | 操作人用户 ID（管理员） |
| `operator_name` | `VARCHAR(50)` | Y | NULL | 操作人姓名快照 |
| `oper_type` | `VARCHAR(30)` | N | — | 操作类型：AUDIT_COMPANION-审核资质 / DISABLE_USER-封禁 / ENABLE_USER-解封 / RESET_PASSWORD-重置密码 / ARBITRATE_ORDER-订单纠纷 / HANDLE_COMPLAINT-处理投诉 / PUBLISH_NOTICE-发布公告 |
| `target_type` | `VARCHAR(20)` | Y | NULL | 目标类型：USER-用户 / ORDER-订单 / COMPANION-陪诊员 / COMPLAINT-投诉 |
| `target_id` | `BIGINT` | Y | NULL | 目标主键 ID |
| `target_desc` | `VARCHAR(200)` | Y | NULL | 目标描述（如订单号 NL20260915000001） |
| `before_status` | `VARCHAR(30)` | Y | NULL | 变更前状态 |
| `after_status` | `VARCHAR(30)` | Y | NULL | 变更后状态 |
| `remark` | `VARCHAR(500)` | Y | NULL | 操作备注 / 原因 |
| `request_url` | `VARCHAR(200)` | Y | NULL | 请求路径 |
| `request_method` | `VARCHAR(10)` | Y | NULL | 请求方法 GET/POST/PUT/DELETE |
| `ip` | `VARCHAR(50)` | Y | NULL | 操作 IP |
| `oper_time` | `DATETIME` | N | — | 操作时间 |
| `create_time` | `DATETIME` | N | CURRENT_TIMESTAMP | 创建时间 |
| `update_time` | `DATETIME` | N | CURRENT_TIMESTAMP | 更新时间 |
| `deleted` | `TINYINT` | N | 0 | 逻辑删除：0-未删除 1-已删除（代码层不提供删改接口） |

**索引**

| 类型 | 名称 | 列 | 说明 |
|---|---|---|---|
| 主键 | `PRIMARY` | `id` | 自增主键 |
| 普通 | `idx_operator_time` | `operator_id`, `oper_time` |  |
| 普通 | `idx_oper_type_time` | `oper_type`, `oper_time` |  |
| 普通 | `idx_target` | `target_type`, `target_id` |  |
| 普通 | `idx_oper_time` | `oper_time` |  |

## 二、老人档案与陪诊员资质域

### 6. `elder_profile` — 老人档案表

- 模块：`M3`　种子数据：**32** 行

| 字段 | 类型 | 可空 | 默认值 | 说明 |
|---|---|---|---|---|
| `id` `PK` | `BIGINT` | N | 自增 | 档案 ID |
| `user_id` | `BIGINT` | Y | NULL | 关联的老人登录账号 ID；由家属代建的档案可为空 |
| `name` | `VARCHAR(50)` | N | — | 姓名 |
| `gender` | `VARCHAR(10)` | N | — | 性别：MALE-男 / FEMALE-女 |
| `birth_date` | `DATE` | N | — | 出生日期（年龄由后端计算，不落库，避免年度失效） |
| `id_card` | `VARCHAR(255)` | Y | NULL | 身份证号（**AES 密文**），接口返回脱敏串，日志禁止打印 |
| `phone` | `VARCHAR(20)` | Y | NULL | 联系电话，返回脱敏 |
| `address` | `VARCHAR(200)` | Y | NULL | 常用地址（文字地址，一期不做地图导航），返回门牌号打码 |
| `emergency_contact` | `VARCHAR(50)` | Y | NULL | 紧急联系人姓名 |
| `emergency_phone` | `VARCHAR(20)` | Y | NULL | 紧急联系人电话，返回脱敏 |
| `medical_history` | `VARCHAR(500)` | Y | NULL | 病史备注（**仅记录，系统不做诊断、不给用药建议**） |
| `allergy_history` | `VARCHAR(500)` | Y | NULL | 过敏史备注（仅记录） |
| `mobility_level` | `VARCHAR(20)` | Y | NULL | 行动能力：SELF-自理 / ASSIST-需搀扶 / WHEELCHAIR-轮椅，用于匹配陪诊员 |
| `favorite_hospital` | `VARCHAR(100)` | Y | NULL | 常去医院 |
| `bind_status` | `VARCHAR(20)` | N | 'UNBOUND' | 绑定状态：BOUND-已绑定家属 / UNBOUND-未绑定 |
| `create_by` | `BIGINT` | Y | NULL | 建档人（家属）用户 ID |
| `remark` | `VARCHAR(255)` | Y | NULL | 备注 |
| `create_time` | `DATETIME` | N | CURRENT_TIMESTAMP | 创建时间 |
| `update_time` | `DATETIME` | N | CURRENT_TIMESTAMP | 更新时间 |
| `deleted` | `TINYINT` | N | 0 | 逻辑删除：0-未删除 1-已删除（历史订单仍关联） |

**索引**

| 类型 | 名称 | 列 | 说明 |
|---|---|---|---|
| 主键 | `PRIMARY` | `id` | 自增主键 |
| 普通 | `idx_user_id` | `user_id` |  |
| 普通 | `idx_bind_status` | `bind_status` |  |
| 普通 | `idx_create_by` | `create_by` |  |
| 普通 | `idx_name` | `name` |  |
| 普通 | `idx_phone` | `phone` |  |

### 7. `family_elder_relation` — 家属-老人绑定关系表

- 模块：`M3`　种子数据：**31** 行

| 字段 | 类型 | 可空 | 默认值 | 说明 |
|---|---|---|---|---|
| `id` `PK` | `BIGINT` | N | 自增 | 关系 ID |
| `family_id` | `BIGINT` | N | — | 家属用户 ID |
| `elder_id` | `BIGINT` | N | — | 老人档案 ID |
| `relation` | `VARCHAR(20)` | N | — | 与老人关系：SON-儿子 / DAUGHTER-女儿 / RELATIVE-亲属 / OTHER-其他 |
| `bind_type` | `VARCHAR(20)` | Y | NULL | 绑定方式：PHONE-按手机号 / INVITE_CODE-按邀请码 |
| `is_default` | `TINYINT` | N | 0 | 是否主要联系人：0-否 1-是 |
| `status` | `VARCHAR(20)` | N | 'BOUND' | 关系状态：BOUND-已绑定 / UNBOUND-已解绑 |
| `bind_time` | `DATETIME` | N | — | 绑定时间 |
| `unbind_time` | `DATETIME` | Y | NULL | 解绑时间 |
| `create_time` | `DATETIME` | N | CURRENT_TIMESTAMP | 创建时间 |
| `update_time` | `DATETIME` | N | CURRENT_TIMESTAMP | 更新时间 |
| `deleted` | `TINYINT` | N | 0 | 逻辑删除：0-未删除 1-已删除 |

**索引**

| 类型 | 名称 | 列 | 说明 |
|---|---|---|---|
| 主键 | `PRIMARY` | `id` | 自增主键 |
| 唯一 | `uk_family_elder` | `family_id`, `elder_id` |  |
| 普通 | `idx_elder_status` | `elder_id`, `status` |  |
| 普通 | `idx_family_status` | `family_id`, `status` |  |

### 8. `companion_audit_record` — 陪诊员资质申请记录表

- 模块：`M9`　种子数据：**34** 行

| 字段 | 类型 | 可空 | 默认值 | 说明 |
|---|---|---|---|---|
| `id` `PK` | `BIGINT` | N | 自增 | 申请 ID |
| `applicant_user_id` | `BIGINT` | N | — | 申请人用户 ID |
| `real_name` | `VARCHAR(50)` | N | — | 真实姓名 |
| `id_card` | `VARCHAR(255)` | Y | NULL | 身份证号（**AES 密文**） |
| `service_area` | `VARCHAR(100)` | N | — | 服务区域，如「海口市美兰区」 |
| `available_time` | `VARCHAR(100)` | N | — | 可服务时段，如「周一至周五 08:00-18:00」 |
| `certificates` | `JSON` | Y | NULL | 证件材料 [{name,url}]，至少 1 项 |
| `apply_remark` | `VARCHAR(200)` | Y | NULL | 申请补充说明 |
| `audit_status` | `VARCHAR(20)` | N | 'PENDING' | 审核状态：PENDING-待审核 / APPROVED-已通过 / REJECTED-已驳回 |
| `reject_reason` | `VARCHAR(200)` | Y | NULL | 驳回原因（驳回时必填，5-200 字） |
| `audit_admin_id` | `BIGINT` | Y | NULL | 审核管理员用户 ID |
| `audit_remark` | `VARCHAR(200)` | Y | NULL | 管理员内部备注 |
| `submit_time` | `DATETIME` | N | — | 提交时间 |
| `audit_time` | `DATETIME` | Y | NULL | 审核时间 |
| `create_time` | `DATETIME` | N | CURRENT_TIMESTAMP | 创建时间 |
| `update_time` | `DATETIME` | N | CURRENT_TIMESTAMP | 更新时间 |
| `deleted` | `TINYINT` | N | 0 | 逻辑删除：0-未删除 1-已删除 |

**索引**

| 类型 | 名称 | 列 | 说明 |
|---|---|---|---|
| 主键 | `PRIMARY` | `id` | 自增主键 |
| 普通 | `idx_applicant_status` | `applicant_user_id`, `audit_status` |  |
| 普通 | `idx_status_submit` | `audit_status`, `submit_time` |  |
| 普通 | `idx_real_name` | `real_name` |  |

### 9. `companion_profile` — 陪诊员业务资料表

- 模块：`M3/M9`　种子数据：**30** 行

| 字段 | 类型 | 可空 | 默认值 | 说明 |
|---|---|---|---|---|
| `id` `PK` | `BIGINT` | N | 自增 | 资料 ID |
| `user_id` `UK` | `BIGINT` | N | — | 关联用户 ID（唯一，一个账号一份资料） |
| `real_name` | `VARCHAR(50)` | N | — | 真实姓名，返回脱敏 |
| `id_card` | `VARCHAR(255)` | Y | NULL | 身份证号（**AES 密文**） |
| `gender` | `VARCHAR(10)` | Y | NULL | 性别：MALE / FEMALE |
| `birth_date` | `DATE` | Y | NULL | 出生日期 |
| `phone` | `VARCHAR(20)` | Y | NULL | 联系电话，返回脱敏 |
| `service_area` | `VARCHAR(100)` | N | — | 服务区域 |
| `available_time` | `VARCHAR(100)` | N | — | 可服务时段 |
| `introduction` | `VARCHAR(500)` | Y | NULL | 个人简介 / 服务说明 |
| `certificate_no` | `VARCHAR(100)` | Y | NULL | 资质证书编号 |
| `health_cert_expire` | `DATE` | Y | NULL | 健康证有效期，过期自动置为不可接单 |
| `audit_status` | `VARCHAR(20)` | N | 'PENDING' | 资质状态：PENDING-待审核 / APPROVED-已通过 / REJECTED-已驳回 |
| `reject_reason` | `VARCHAR(200)` | Y | NULL | 最近一次驳回原因 |
| `audit_admin_id` | `BIGINT` | Y | NULL | 审核管理员用户 ID |
| `audit_time` | `DATETIME` | Y | NULL | 审核时间 |
| `work_status` | `VARCHAR(20)` | N | 'AVAILABLE' | 接单状态：AVAILABLE-可接单 / REST-休息中 |
| `score` | `DECIMAL(3,2)` | N | 0.00 | 平均评分（0.00-5.00），由评价聚合后冗余更新，避免实时聚合 |
| `review_count` | `INT` | N | 0 | 有效评价数 |
| `order_count` | `INT` | N | 0 | 累计已完成订单数（仅 COMPLETED/REVIEWED 计入） |
| `accept_count` | `INT` | N | 0 | 累计接单数（含进行中） |
| `remark` | `VARCHAR(255)` | Y | NULL | 备注 |
| `create_time` | `DATETIME` | N | CURRENT_TIMESTAMP | 创建时间 |
| `update_time` | `DATETIME` | N | CURRENT_TIMESTAMP | 更新时间 |
| `deleted` | `TINYINT` | N | 0 | 逻辑删除：0-未删除 1-已删除 |

**索引**

| 类型 | 名称 | 列 | 说明 |
|---|---|---|---|
| 主键 | `PRIMARY` | `id` | 自增主键 |
| 唯一 | `uk_user_id` | `user_id` |  |
| 普通 | `idx_audit_work` | `audit_status`, `work_status` |  |
| 普通 | `idx_service_area` | `service_area` |  |
| 普通 | `idx_score` | `score` |  |
| 普通 | `idx_order_count` | `order_count` |  |

## 三、陪诊订单与执行域（系统核心）

### 10. `companion_order` — 陪诊订单主表

- 模块：`M4`　种子数据：**64** 行

| 字段 | 类型 | 可空 | 默认值 | 说明 |
|---|---|---|---|---|
| `id` `PK` | `BIGINT` | N | 自增 | 订单 ID |
| `order_no` `UK` | `VARCHAR(32)` | N | — | 订单号，格式 NL + yyyyMMdd + 6 位序列，如 NL20260915000001 |
| `family_id` | `BIGINT` | N | — | 下单家属用户 ID |
| `elder_id` | `BIGINT` | N | — | 就诊老人档案 ID |
| `companion_id` | `BIGINT` | Y | NULL | 接单陪诊员用户 ID，未接单为 NULL |
| `hospital` | `VARCHAR(100)` | N | — | 医院名称 |
| `department` | `VARCHAR(50)` | N | — | 就诊科室 |
| `visit_time` | `DATETIME` | N | — | 就诊时间，必须晚于下单时间 |
| `address` | `VARCHAR(200)` | N | — | 医院地址（**文字地址，一期不做地图导航**） |
| `longitude` | `DECIMAL(10,6)` | Y | NULL | 订单地址经度，用于打卡距离校验（不渲染地图） |
| `latitude` | `DECIMAL(10,6)` | Y | NULL | 订单地址纬度 |
| `remark` | `VARCHAR(500)` | Y | NULL | 家属备注（如「老人听力不好，请大声沟通」） |
| `status` | `VARCHAR(20)` | N | 'PENDING' | 订单状态：PENDING-待接单 / ACCEPTED-已接单 / IN_SERVICE-服务中 / COMPLETED-已完成 / REVIEWED-已评价 / CANCELLED-已取消 |
| `fee` | `DECIMAL(10,2)` | N | 0.00 | 服务费（元），一期不做在线支付，仅线上记账 + 线下结算 |
| `actual_fee` | `DECIMAL(10,2)` | Y | NULL | 实际结算金额（线下结算回填） |
| `payment_status` | `VARCHAR(20)` | N | 'UNPAID' | 结算状态：UNPAID-未结算 / SETTLED-已结算 |
| `service_summary` | `VARCHAR(500)` | Y | NULL | 服务小结（**只记录过程，禁止出现诊断与用药建议**） |
| `service_photos` | `JSON` | Y | NULL | 服务现场/取药凭证照片 URL 数组 |
| `accept_time` | `DATETIME` | Y | NULL | 接单时间 |
| `start_time` | `DATETIME` | Y | NULL | 开始服务时间 |
| `finish_time` | `DATETIME` | Y | NULL | 完成时间 |
| `cancel_time` | `DATETIME` | Y | NULL | 取消时间 |
| `cancel_reason` | `VARCHAR(200)` | Y | NULL | 取消/纠纷原因 |
| `cancel_by` | `BIGINT` | Y | NULL | 取消操作人用户 ID |
| `arbitrate_flag` | `TINYINT` | N | 0 | 是否被管理员强制处理：0-否 1-是 |
| `arbitrate_result` | `VARCHAR(500)` | Y | NULL | 管理员纠纷处理结果说明 |
| `version` | `INT` | N | 0 | 乐观锁版本号（**接单防超卖**，MyBatis-Plus @Version） |
| `create_time` | `DATETIME` | N | CURRENT_TIMESTAMP | 下单时间 |
| `update_time` | `DATETIME` | N | CURRENT_TIMESTAMP | 更新时间 |
| `deleted` | `TINYINT` | N | 0 | 逻辑删除：0-未删除 1-已删除 |

**索引**

| 类型 | 名称 | 列 | 说明 |
|---|---|---|---|
| 主键 | `PRIMARY` | `id` | 自增主键 |
| 唯一 | `uk_order_no` | `order_no` |  |
| 普通 | `idx_status_create` | `status`, `create_time` |  |
| 普通 | `idx_status_visit` | `status`, `visit_time` | 订单大厅：status = 'PENDING' AND visit_time > NOW() ORDER BY visit_time |
| 普通 | `idx_family_create` | `family_id`, `create_time` |  |
| 普通 | `idx_companion_status` | `companion_id`, `status` |  |
| 普通 | `idx_elder_id` | `elder_id` |  |
| 普通 | `idx_visit_time` | `visit_time` |  |
| 普通 | `idx_payment_status` | `payment_status` |  |
| 普通 | `idx_create_time` | `create_time` |  |

### 11. `order_status_log` — 订单状态流转日志表（只增不改）

- 模块：`M4`　种子数据：**246** 行

| 字段 | 类型 | 可空 | 默认值 | 说明 |
|---|---|---|---|---|
| `id` `PK` | `BIGINT` | N | 自增 | 日志 ID |
| `order_id` | `BIGINT` | N | — | 订单 ID |
| `from_status` | `VARCHAR(20)` | Y | NULL | 变更前状态，首次下单为 NULL |
| `to_status` | `VARCHAR(20)` | N | — | 变更后状态 |
| `operator_id` | `BIGINT` | Y | NULL | 操作人用户 ID，系统自动流转为 NULL |
| `operator_name` | `VARCHAR(50)` | Y | NULL | 操作人姓名快照 |
| `operator_role` | `VARCHAR(20)` | Y | NULL | 操作人角色：ELDER / FAMILY / COMPANION / ADMIN / SYSTEM |
| `remark` | `VARCHAR(500)` | Y | NULL | 备注（如「管理员强制变更」） |
| `is_force` | `TINYINT` | N | 0 | 是否管理员强制变更：0-否 1-是 |
| `operate_time` | `DATETIME` | N | — | 发生时间 |
| `create_time` | `DATETIME` | N | CURRENT_TIMESTAMP | 创建时间 |
| `update_time` | `DATETIME` | N | CURRENT_TIMESTAMP | 更新时间 |
| `deleted` | `TINYINT` | N | 0 | 逻辑删除：0-未删除 1-已删除 |

**索引**

| 类型 | 名称 | 列 | 说明 |
|---|---|---|---|
| 主键 | `PRIMARY` | `id` | 自增主键 |
| 普通 | `idx_order_time` | `order_id`, `operate_time` |  |
| 普通 | `idx_to_status` | `to_status` |  |
| 普通 | `idx_operator` | `operator_id` |  |

### 12. `order_reject_log` — 陪诊员拒单记录表

- 模块：`M4`　种子数据：**36** 行

| 字段 | 类型 | 可空 | 默认值 | 说明 |
|---|---|---|---|---|
| `id` `PK` | `BIGINT` | N | 自增 | 记录 ID |
| `order_id` | `BIGINT` | N | — | 订单 ID |
| `companion_id` | `BIGINT` | N | — | 拒单陪诊员用户 ID |
| `reason` | `VARCHAR(200)` | Y | NULL | 拒单原因 |
| `reject_time` | `DATETIME` | N | — | 拒单时间 |
| `create_time` | `DATETIME` | N | CURRENT_TIMESTAMP | 创建时间 |
| `update_time` | `DATETIME` | N | CURRENT_TIMESTAMP | 更新时间 |
| `deleted` | `TINYINT` | N | 0 | 逻辑删除：0-未删除 1-已删除 |

**索引**

| 类型 | 名称 | 列 | 说明 |
|---|---|---|---|
| 主键 | `PRIMARY` | `id` | 自增主键 |
| 唯一 | `uk_order_companion` | `order_id`, `companion_id` |  |
| 普通 | `idx_companion_id` | `companion_id` |  |

### 13. `order_checkin` — 陪诊打卡记录表

- 模块：`M5`　种子数据：**272** 行

| 字段 | 类型 | 可空 | 默认值 | 说明 |
|---|---|---|---|---|
| `id` `PK` | `BIGINT` | N | 自增 | 打卡记录 ID |
| `order_id` | `BIGINT` | N | — | 订单 ID |
| `companion_id` | `BIGINT` | N | — | 打卡陪诊员用户 ID |
| `node` | `VARCHAR(20)` | N | — | 打卡节点：DEPART-出发 / ARRIVE-到院 / IN_CONSULT-就诊中 / TAKE_MEDICINE-取药 / LEAVE-离院 / FINISH-完成 |
| `node_sort` | `TINYINT` | N | — | 节点顺序值 1-6，用于校验「可跳过不可回退」 |
| `longitude` | `DECIMAL(10,6)` | N | — | 打卡经度 |
| `latitude` | `DECIMAL(10,6)` | N | — | 打卡纬度 |
| `address` | `VARCHAR(200)` | Y | NULL | 打卡位置文字描述（一期由前端传入或留空，不做逆地理编码） |
| `distance` | `INT` | Y | NULL | 与订单地址的直线距离（米），Haversine 计算 |
| `is_abnormal` | `TINYINT` | N | 0 | 是否异常打卡（超出阈值）：0-正常 1-异常 |
| `photos` | `JSON` | Y | NULL | 现场照片 URL 数组，最多 6 张 |
| `remark` | `VARCHAR(200)` | Y | NULL | 备注 |
| `checkin_time` | `DATETIME` | N | — | 打卡时间 |
| `create_time` | `DATETIME` | N | CURRENT_TIMESTAMP | 创建时间 |
| `update_time` | `DATETIME` | N | CURRENT_TIMESTAMP | 更新时间 |
| `deleted` | `TINYINT` | N | 0 | 逻辑删除：0-未删除 1-已删除 |

**索引**

| 类型 | 名称 | 列 | 说明 |
|---|---|---|---|
| 主键 | `PRIMARY` | `id` | 自增主键 |
| 唯一 | `uk_order_node` | `order_id`, `node` |  |
| 普通 | `idx_order_time` | `order_id`, `checkin_time` |  |
| 普通 | `idx_companion_id` | `companion_id` |  |

### 14. `companion_track` — 陪诊轨迹点表

- 模块：`M5`　种子数据：**544** 行

| 字段 | 类型 | 可空 | 默认值 | 说明 |
|---|---|---|---|---|
| `id` `PK` | `BIGINT` | N | 自增 | 轨迹点 ID |
| `order_id` | `BIGINT` | N | — | 订单 ID |
| `companion_id` | `BIGINT` | N | — | 陪诊员用户 ID |
| `node` | `VARCHAR(20)` | Y | NULL | 关联打卡节点，非打卡产生的点为 NULL |
| `longitude` | `DECIMAL(10,6)` | N | — | 经度 |
| `latitude` | `DECIMAL(10,6)` | N | — | 纬度 |
| `accuracy` | `DECIMAL(6,2)` | Y | NULL | 定位精度（米），用于剔除漂移点 |
| `speed` | `DECIMAL(6,2)` | Y | NULL | 速度（米/秒），用于判断是否在移动 |
| `record_time` | `DATETIME` | N | — | 定位记录时间 |
| `create_time` | `DATETIME` | N | CURRENT_TIMESTAMP | 创建时间 |
| `update_time` | `DATETIME` | N | CURRENT_TIMESTAMP | 更新时间 |
| `deleted` | `TINYINT` | N | 0 | 逻辑删除：0-未删除 1-已删除 |

**索引**

| 类型 | 名称 | 列 | 说明 |
|---|---|---|---|
| 主键 | `PRIMARY` | `id` | 自增主键 |
| 普通 | `idx_order_time` | `order_id`, `record_time` |  |
| 普通 | `idx_companion_id` | `companion_id` |  |

## 四、用药管理域

### 15. `medicine_dict` — 药品字典表

- 模块：`M6`　种子数据：**61** 行

| 字段 | 类型 | 可空 | 默认值 | 说明 |
|---|---|---|---|---|
| `id` `PK` | `BIGINT` | N | 自增 | 药品 ID |
| `name` | `VARCHAR(100)` | N | — | 通用名，如「苯磺酸氨氯地平片」 |
| `trade_name` | `VARCHAR(100)` | Y | NULL | 商品名，如「络活喜」，可为空 |
| `specification` | `VARCHAR(100)` | Y | NULL | 规格，如「5mg × 28 片」 |
| `dosage_form` | `VARCHAR(20)` | N | — | 剂型：TABLET-片剂 / CAPSULE-胶囊 / INJECTION-注射剂 / LIQUID-口服液 / OTHER-其他 |
| `common_usage` | `VARCHAR(500)` | Y | NULL | 通用服用说明（**仅「口服，具体用法用量请遵医嘱」这类通用表述**） |
| `precautions` | `VARCHAR(500)` | Y | NULL | 注意事项（如「可能引起嗜睡」「需避光保存」） |
| `storage` | `VARCHAR(200)` | Y | NULL | 储存条件 |
| `manufacturer` | `VARCHAR(100)` | Y | NULL | 生产厂家 |
| `approval_no` | `VARCHAR(50)` | Y | NULL | 批准文号 |
| `otc_type` | `VARCHAR(20)` | Y | NULL | 处方类别：OTC-非处方药 / PRESCRIPTION-处方药 |
| `is_common` | `TINYINT` | N | 0 | 是否常用药（前端优先展示）：0-否 1-是 |
| `disclaimer` | `VARCHAR(500)` | N | — | 免责声明（**接口必返、前端必展示**） |
| `create_time` | `DATETIME` | N | CURRENT_TIMESTAMP | 创建时间 |
| `update_time` | `DATETIME` | N | CURRENT_TIMESTAMP | 更新时间 |
| `deleted` | `TINYINT` | N | 0 | 逻辑删除：0-未删除 1-已删除 |

**索引**

| 类型 | 名称 | 列 | 说明 |
|---|---|---|---|
| 主键 | `PRIMARY` | `id` | 自增主键 |
| 普通 | `idx_name` | `name` |  |
| 普通 | `idx_trade_name` | `trade_name` |  |
| 普通 | `idx_dosage_form` | `dosage_form` |  |
| 普通 | `idx_is_common` | `is_common` |  |

### 16. `medication_plan` — 用药计划表

- 模块：`M6`　种子数据：**36** 行

| 字段 | 类型 | 可空 | 默认值 | 说明 |
|---|---|---|---|---|
| `id` `PK` | `BIGINT` | N | 自增 | 计划 ID |
| `elder_id` | `BIGINT` | N | — | 老人档案 ID |
| `medicine_id` | `BIGINT` | N | — | 药品 ID |
| `medicine_name` | `VARCHAR(100)` | N | — | 药品通用名快照（药品字典变更后历史计划仍可读） |
| `dosage` | `VARCHAR(50)` | N | — | 单次用量，如「1 片」（**家属按医嘱填写**） |
| `frequency` | `TINYINT` | N | — | 每日次数 1-4 |
| `time_points` | `VARCHAR(100)` | N | — | 服药时间点 JSON 数组，如 ["08:00","12:00","18:00"]，长度须等于 frequency |
| `start_date` | `DATE` | N | — | 开始日期 |
| `end_date` | `DATE` | Y | NULL | 结束日期，NULL 表示长期用药 |
| `meal_relation` | `VARCHAR(20)` | N | — | 与饭点关系：BEFORE_MEAL-饭前 / AFTER_MEAL-饭后 / ANY-不限 |
| `status` | `VARCHAR(20)` | N | 'ACTIVE' | 计划状态：ACTIVE-进行中 / DISABLED-已停用（**不物理删除，保留服药历史**） |
| `remark` | `VARCHAR(200)` | Y | NULL | 备注（如「医生让吃两周」） |
| `created_by` | `BIGINT` | N | — | 创建人（家属）用户 ID |
| `create_time` | `DATETIME` | N | CURRENT_TIMESTAMP | 创建时间 |
| `update_time` | `DATETIME` | N | CURRENT_TIMESTAMP | 更新时间 |
| `deleted` | `TINYINT` | N | 0 | 逻辑删除：0-未删除 1-已删除 |

**索引**

| 类型 | 名称 | 列 | 说明 |
|---|---|---|---|
| 主键 | `PRIMARY` | `id` | 自增主键 |
| 普通 | `idx_elder_status` | `elder_id`, `status` |  |
| 普通 | `idx_medicine_id` | `medicine_id` |  |
| 普通 | `idx_start_end` | `start_date`, `end_date` |  |
| 普通 | `idx_created_by` | `created_by` |  |

### 17. `medication_task` — 每日服药任务表

- 模块：`M6`　种子数据：**520** 行

| 字段 | 类型 | 可空 | 默认值 | 说明 |
|---|---|---|---|---|
| `id` `PK` | `BIGINT` | N | 自增 | 任务 ID |
| `plan_id` | `BIGINT` | N | — | 所属用药计划 ID |
| `elder_id` | `BIGINT` | N | — | 老人档案 ID（冗余，避免查任务时再关联计划表） |
| `medicine_id` | `BIGINT` | N | — | 药品 ID（冗余） |
| `medicine_name` | `VARCHAR(100)` | N | — | 药品名快照 |
| `dosage` | `VARCHAR(50)` | Y | NULL | 单次用量快照 |
| `meal_relation` | `VARCHAR(20)` | Y | NULL | 饭点关系快照（历史任务不受计划修改影响） |
| `plan_date` | `DATE` | N | — | 计划日期（按天聚合用） |
| `plan_time` | `DATETIME` | N | — | 计划服药时间（精确到分钟） |
| `status` | `VARCHAR(20)` | N | 'PENDING' | 任务状态：PENDING-待服用 / TAKEN-已服用 / MISSED-漏服 |
| `was_missed` | `TINYINT` | N | 0 | 是否曾判定漏服后补记：0-否 1-是（统计漏服率用） |
| `confirm_time` | `DATETIME` | Y | NULL | 实际确认/服药时间 |
| `confirm_by` | `BIGINT` | Y | NULL | 确认人用户 ID |
| `confirm_remark` | `VARCHAR(200)` | Y | NULL | 确认备注（如「今天外出，晚了 1 小时」） |
| `notify_sent` | `TINYINT` | N | 0 | 漏服提醒是否已推送：0-否 1-是（防重复推送） |
| `notify_time` | `DATETIME` | Y | NULL | 提醒推送时间 |
| `create_time` | `DATETIME` | N | CURRENT_TIMESTAMP | 创建时间 |
| `update_time` | `DATETIME` | N | CURRENT_TIMESTAMP | 更新时间 |
| `deleted` | `TINYINT` | N | 0 | 逻辑删除：0-未删除 1-已删除 |

**索引**

| 类型 | 名称 | 列 | 说明 |
|---|---|---|---|
| 主键 | `PRIMARY` | `id` | 自增主键 |
| 唯一 | `uk_plan_time` | `plan_id`, `plan_time` |  |
| 普通 | `idx_elder_date_status` | `elder_id`, `plan_date`, `status` |  |
| 普通 | `idx_status_plan_time` | `status`, `plan_time` |  |
| 普通 | `idx_plan_id` | `plan_id` |  |
| 普通 | `idx_plan_date` | `plan_date` |  |

## 五、评价、投诉与消息域

### 18. `order_review` — 订单评价表

- 模块：`M7`　种子数据：**31** 行

| 字段 | 类型 | 可空 | 默认值 | 说明 |
|---|---|---|---|---|
| `id` `PK` | `BIGINT` | N | 自增 | 评价 ID |
| `order_id` `UK` | `BIGINT` | N | — | 订单 ID（唯一，一单一评） |
| `order_no` | `VARCHAR(32)` | Y | NULL | 订单号快照 |
| `family_id` | `BIGINT` | N | — | 评价人（下单家属）用户 ID |
| `elder_id` | `BIGINT` | Y | NULL | 被服务老人档案 ID |
| `companion_id` | `BIGINT` | N | — | 被评价陪诊员用户 ID |
| `score` | `TINYINT` | N | — | 评分 1-5 星 |
| `tags` | `VARCHAR(200)` | Y | NULL | 评价标签 JSON 数组，如 ["准时","耐心"]，最多 5 个 |
| `content` | `VARCHAR(500)` | Y | NULL | 评价文字，5-500 字 |
| `is_anonymous` | `TINYINT` | N | 0 | 是否匿名：0-否 1-是（匿名时列表与详情均不暴露家属姓名） |
| `companion_reply` | `VARCHAR(500)` | Y | NULL | 陪诊员回复 |
| `reply_time` | `DATETIME` | Y | NULL | 回复时间 |
| `is_valid` | `TINYINT` | N | 1 | 是否有效评价：0-管理员判定无效（不计入评分聚合）1-有效 |
| `create_time` | `DATETIME` | N | CURRENT_TIMESTAMP | 评价时间 |
| `update_time` | `DATETIME` | N | CURRENT_TIMESTAMP | 更新时间 |
| `deleted` | `TINYINT` | N | 0 | 逻辑删除：0-未删除 1-已删除 |

**索引**

| 类型 | 名称 | 列 | 说明 |
|---|---|---|---|
| 主键 | `PRIMARY` | `id` | 自增主键 |
| 唯一 | `uk_order_id` | `order_id` |  |
| 普通 | `idx_companion_valid` | `companion_id`, `is_valid` |  |
| 普通 | `idx_family_id` | `family_id` |  |
| 普通 | `idx_score` | `score` |  |
| 普通 | `idx_create_time` | `create_time` |  |

### 19. `complaint` — 投诉表

- 模块：`M7`　种子数据：**32** 行

| 字段 | 类型 | 可空 | 默认值 | 说明 |
|---|---|---|---|---|
| `id` `PK` | `BIGINT` | N | 自增 | 投诉 ID |
| `order_id` | `BIGINT` | N | — | 关联订单 ID |
| `order_no` | `VARCHAR(32)` | Y | NULL | 订单号快照 |
| `complainant_id` | `BIGINT` | N | — | 投诉人用户 ID |
| `complainant_role` | `VARCHAR(20)` | Y | NULL | 投诉人角色 |
| `target_user_id` | `BIGINT` | N | — | 被投诉人用户 ID |
| `target_role` | `VARCHAR(20)` | Y | NULL | 被投诉人角色 |
| `type` | `VARCHAR(20)` | N | — | 投诉类型：LATE-迟到/未按时到达 / ATTITUDE-服务态度差 / INCOMPLETE-服务未完成 / FEE_DISPUTE-费用纠纷 / PRIVACY-隐私泄露 / OTHER-其他 |
| `content` | `VARCHAR(1000)` | N | — | 投诉内容，10-1000 字 |
| `evidence` | `JSON` | Y | NULL | 证据材料 URL 数组，最多 6 张 |
| `status` | `VARCHAR(20)` | N | 'PENDING' | 处理状态：PENDING-待处理 / PROCESSING-处理中 / RESOLVED-已结案 / REJECTED-已驳回（**只可正向流转**） |
| `handle_admin_id` | `BIGINT` | Y | NULL | 处理管理员用户 ID |
| `handle_result` | `VARCHAR(500)` | Y | NULL | 处理结果说明（10-500 字） |
| `penalty_to_target` | `TINYINT` | N | 0 | 是否对被投诉人计违规：0-否 1-是 |
| `handle_time` | `DATETIME` | Y | NULL | 处理时间 |
| `create_time` | `DATETIME` | N | CURRENT_TIMESTAMP | 提交时间 |
| `update_time` | `DATETIME` | N | CURRENT_TIMESTAMP | 更新时间 |
| `deleted` | `TINYINT` | N | 0 | 逻辑删除：0-未删除 1-已删除 |

**索引**

| 类型 | 名称 | 列 | 说明 |
|---|---|---|---|
| 主键 | `PRIMARY` | `id` | 自增主键 |
| 普通 | `idx_order_id` | `order_id` |  |
| 普通 | `idx_status_create` | `status`, `create_time` |  |
| 普通 | `idx_complainant` | `complainant_id` |  |
| 普通 | `idx_target_user` | `target_user_id` |  |
| 普通 | `idx_type` | `type` |  |

### 20. `internal_message` — 站内信表

- 模块：`M8`　种子数据：**75** 行

| 字段 | 类型 | 可空 | 默认值 | 说明 |
|---|---|---|---|---|
| `id` `PK` | `BIGINT` | N | 自增 | 消息 ID |
| `receiver_id` | `BIGINT` | N | — | 接收人用户 ID |
| `sender_id` | `BIGINT` | Y | NULL | 发送人用户 ID，NULL 表示系统发送 |
| `type` | `VARCHAR(30)` | N | — | 消息类型：ORDER_CREATED / ORDER_ACCEPTED / ORDER_PROGRESS / ORDER_COMPLETED / ORDER_CANCELLED / AUDIT_RESULT / MEDICATION_REMIND / COMPLAINT_HANDLED / SYSTEM_NOTICE |
| `title` | `VARCHAR(50)` | N | — | 标题，最长 50 字 |
| `content` | `VARCHAR(500)` | N | — | 正文，最长 500 字，**由后端模板填充并脱敏，前端禁止拼文案** |
| `biz_type` | `VARCHAR(20)` | Y | NULL | 关联业务类型：ORDER-订单 / AUDIT-资质 / MEDICATION-用药 / COMPLAINT-投诉 / SYSTEM-系统 |
| `biz_id` | `BIGINT` | Y | NULL | 关联业务 ID，用于点击跳转 |
| `link_url` | `VARCHAR(200)` | Y | NULL | 前端跳转路径，如 /family?orderId=1001 |
| `is_read` | `TINYINT` | N | 0 | 是否已读：0-未读 1-已读 |
| `read_time` | `DATETIME` | Y | NULL | 阅读时间 |
| `receiver_deleted` | `TINYINT` | N | 0 | 接收人侧删除标记：0-正常 1-已删除（仅影响接收人视图） |
| `create_time` | `DATETIME` | N | CURRENT_TIMESTAMP | 发送时间 |
| `update_time` | `DATETIME` | N | CURRENT_TIMESTAMP | 更新时间 |
| `deleted` | `TINYINT` | N | 0 | 逻辑删除：0-未删除 1-已删除 |

**索引**

| 类型 | 名称 | 列 | 说明 |
|---|---|---|---|
| 主键 | `PRIMARY` | `id` | 自增主键 |
| 普通 | `idx_receiver_read` | `receiver_id`, `is_read`, `create_time` |  |
| 普通 | `idx_receiver_type` | `receiver_id`, `type` |  |
| 普通 | `idx_biz` | `biz_type`, `biz_id` |  |
| 普通 | `idx_create_time` | `create_time` |  |
