# 银龄伴诊 · ER 图与实体关系（M1）

> mermaid 语法，支持 GitHub / VS Code / Typora 直接渲染。

## 一、实体关系总览（逻辑模型）

```mermaid
erDiagram
    sys_user ||--o{ companion_order : "家属下单（一对多）"
    sys_user ||--o{ companion_order : "陪诊员接单（一对多，可空）"
    elder_profile ||--o{ companion_order : "老人被服务（一对多）"
    sys_user ||--o| elder_profile : "老人账号关联档案（一对一，可空）"
    sys_user ||--o| companion_profile : "陪诊员账号关联资料（一对一）"
    sys_user ||--o{ family_elder_relation : "家属绑定关系"
    elder_profile ||--o{ family_elder_relation : "老人被绑定"
    companion_order ||--o{ order_status_log : "状态流转留痕"
    companion_order ||--o{ order_checkin : "打卡记录"
    companion_order ||--o{ companion_track : "轨迹点"
    companion_order ||--o| order_review : "一单一评"
    companion_order ||--o{ complaint : "订单投诉"
    elder_profile ||--o{ medication_plan : "老人用药计划"
    medicine_dict ||--o{ medication_plan : "药品被引用"
    medication_plan ||--o{ medication_task : "计划生成每日任务"
    sys_user ||--o{ internal_message : "接收站内信"
    sys_user ||--o{ companion_audit_record : "陪诊员资质申请"
```

## 二、关系明细

| 主表 | 从表 | 外键列 | 基数 | 说明 |
|---|---|---|---|---|
| `sys_user` | `companion_order` | `family_id` | 1 : N | 家属下单（一对多） |
| `sys_user` | `companion_order` | `companion_id` | 1 : N | 陪诊员接单（一对多，可空） |
| `elder_profile` | `companion_order` | `elder_id` | 1 : N | 老人被服务（一对多） |
| `sys_user` | `elder_profile` | `user_id` | 1 : 1 或 0 | 老人账号关联档案（一对一，可空） |
| `sys_user` | `companion_profile` | `user_id` | 1 : 1 或 0 | 陪诊员账号关联资料（一对一） |
| `sys_user` | `family_elder_relation` | `family_id` | 1 : N | 家属绑定关系 |
| `elder_profile` | `family_elder_relation` | `elder_id` | 1 : N | 老人被绑定 |
| `companion_order` | `order_status_log` | `order_id` | 1 : N | 状态流转留痕 |
| `companion_order` | `order_checkin` | `order_id` | 1 : N | 打卡记录 |
| `companion_order` | `companion_track` | `order_id` | 1 : N | 轨迹点 |
| `companion_order` | `order_review` | `order_id` | 1 : 1 或 0 | 一单一评 |
| `companion_order` | `complaint` | `order_id` | 1 : N | 订单投诉 |
| `elder_profile` | `medication_plan` | `elder_id` | 1 : N | 老人用药计划 |
| `medicine_dict` | `medication_plan` | `medicine_id` | 1 : N | 药品被引用 |
| `medication_plan` | `medication_task` | `plan_id` | 1 : N | 计划生成每日任务 |
| `sys_user` | `internal_message` | `receiver_id` | 1 : N | 接收站内信 |
| `sys_user` | `companion_audit_record` | `applicant_user_id` | 1 : N | 陪诊员资质申请 |

> **不建物理外键**。原因：① 课程设计需演示并发接单、批量导入，外键会加剧锁竞争；
> ② 逻辑删除（`deleted=1`）与物理外键的 `ON DELETE` 语义冲突；
> ③ 参照完整性由 Service 层 + 唯一索引共同保证。详见 `03-索引设计与EXPLAIN.md`。

## 三、分域 ER 图（物理模型，含字段）

### 3.1 一、账号与权限域

```mermaid
erDiagram
    sys_user {
        bigint id PK
        varchar_50 username UK
        varchar_100 password
        varchar_50 nickname
        varchar_50 real_name
        varchar_20 phone UK
        varchar_255 avatar
        varchar_20 role
        varchar_20 status
        tinyint need_change_password
        datetime last_login_time
        varchar_50 last_login_ip
        varchar_255 remark
        datetime create_time
        datetime update_time
        tinyint deleted
    }
    sys_login_log {
        bigint id PK
        bigint user_id
        varchar_50 username
        varchar_20 login_type
        varchar_20 status
        varchar_200 fail_reason
        varchar_50 ip
        varchar_255 user_agent
        datetime login_time
        datetime create_time
        datetime update_time
        tinyint deleted
    }
    sys_dict {
        bigint id PK
        varchar_50 dict_type
        varchar_50 dict_code
        varchar_100 dict_label
        varchar_100 dict_value
        int sort_no
        tinyint status
        varchar_255 remark
        datetime create_time
        datetime update_time
        tinyint deleted
    }
    sys_file {
        bigint id PK
        varchar_64 file_id UK
        varchar_255 origin_name
        varchar_255 stored_name
        varchar_255 url
        varchar_255 store_path
        bigint size
        varchar_20 ext
        varchar_100 content_type
        varchar_20 biz_type
        bigint biz_id
        bigint uploader_id
        datetime create_time
        datetime update_time
        tinyint deleted
    }
    admin_oper_log {
        bigint id PK
        bigint operator_id
        varchar_50 operator_name
        varchar_30 oper_type
        varchar_20 target_type
        bigint target_id
        varchar_200 target_desc
        varchar_30 before_status
        varchar_30 after_status
        varchar_500 remark
        varchar_200 request_url
        varchar_10 request_method
        varchar_50 ip
        datetime oper_time
        datetime create_time
        datetime update_time
        tinyint deleted
    }
```

### 3.2 二、老人档案与陪诊员资质域

```mermaid
erDiagram
    elder_profile {
        bigint id PK
        bigint user_id
        varchar_50 name
        varchar_10 gender
        date birth_date
        varchar_255 id_card
        varchar_20 phone
        varchar_200 address
        varchar_50 emergency_contact
        varchar_20 emergency_phone
        varchar_500 medical_history
        varchar_500 allergy_history
        varchar_20 mobility_level
        varchar_100 favorite_hospital
        varchar_20 bind_status
        bigint create_by
        varchar_255 remark
        datetime create_time
        datetime update_time
        tinyint deleted
    }
    family_elder_relation {
        bigint id PK
        bigint family_id
        bigint elder_id
        varchar_20 relation
        varchar_20 bind_type
        tinyint is_default
        varchar_20 status
        datetime bind_time
        datetime unbind_time
        datetime create_time
        datetime update_time
        tinyint deleted
    }
    companion_audit_record {
        bigint id PK
        bigint applicant_user_id
        varchar_50 real_name
        varchar_255 id_card
        varchar_100 service_area
        varchar_100 available_time
        json certificates
        varchar_200 apply_remark
        varchar_20 audit_status
        varchar_200 reject_reason
        bigint audit_admin_id
        varchar_200 audit_remark
        datetime submit_time
        datetime audit_time
        datetime create_time
        datetime update_time
        tinyint deleted
    }
    companion_profile {
        bigint id PK
        bigint user_id UK
        varchar_50 real_name
        varchar_255 id_card
        varchar_10 gender
        date birth_date
        varchar_20 phone
        varchar_100 service_area
        varchar_100 available_time
        varchar_500 introduction
        varchar_100 certificate_no
        date health_cert_expire
        varchar_20 audit_status
        varchar_200 reject_reason
        bigint audit_admin_id
        datetime audit_time
        varchar_20 work_status
        decimal_3_2 score
        int review_count
        int order_count
        int accept_count
        varchar_255 remark
        datetime create_time
        datetime update_time
        tinyint deleted
    }
    elder_profile ||--o{ family_elder_relation : "elder_id"
```

### 3.3 三、陪诊订单与执行域（系统核心）

```mermaid
erDiagram
    companion_order {
        bigint id PK
        varchar_32 order_no UK
        bigint family_id
        bigint elder_id
        bigint companion_id
        varchar_100 hospital
        varchar_50 department
        datetime visit_time
        varchar_200 address
        decimal_10_6 longitude
        decimal_10_6 latitude
        varchar_500 remark
        varchar_20 status
        decimal_10_2 fee
        decimal_10_2 actual_fee
        varchar_20 payment_status
        varchar_500 service_summary
        json service_photos
        datetime accept_time
        datetime start_time
        datetime finish_time
        datetime cancel_time
        varchar_200 cancel_reason
        bigint cancel_by
        tinyint arbitrate_flag
        varchar_500 arbitrate_result
        int version
        datetime create_time
        datetime update_time
        tinyint deleted
    }
    order_status_log {
        bigint id PK
        bigint order_id
        varchar_20 from_status
        varchar_20 to_status
        bigint operator_id
        varchar_50 operator_name
        varchar_20 operator_role
        varchar_500 remark
        tinyint is_force
        datetime operate_time
        datetime create_time
        datetime update_time
        tinyint deleted
    }
    order_reject_log {
        bigint id PK
        bigint order_id
        bigint companion_id
        varchar_200 reason
        datetime reject_time
        datetime create_time
        datetime update_time
        tinyint deleted
    }
    order_checkin {
        bigint id PK
        bigint order_id
        bigint companion_id
        varchar_20 node
        tinyint node_sort
        decimal_10_6 longitude
        decimal_10_6 latitude
        varchar_200 address
        int distance
        tinyint is_abnormal
        json photos
        varchar_200 remark
        datetime checkin_time
        datetime create_time
        datetime update_time
        tinyint deleted
    }
    companion_track {
        bigint id PK
        bigint order_id
        bigint companion_id
        varchar_20 node
        decimal_10_6 longitude
        decimal_10_6 latitude
        decimal_6_2 accuracy
        decimal_6_2 speed
        datetime record_time
        datetime create_time
        datetime update_time
        tinyint deleted
    }
    companion_order ||--o{ order_status_log : "order_id"
    companion_order ||--o{ order_checkin : "order_id"
    companion_order ||--o{ companion_track : "order_id"
```

### 3.4 四、用药管理域

```mermaid
erDiagram
    medicine_dict {
        bigint id PK
        varchar_100 name
        varchar_100 trade_name
        varchar_100 specification
        varchar_20 dosage_form
        varchar_500 common_usage
        varchar_500 precautions
        varchar_200 storage
        varchar_100 manufacturer
        varchar_50 approval_no
        varchar_20 otc_type
        tinyint is_common
        varchar_500 disclaimer
        datetime create_time
        datetime update_time
        tinyint deleted
    }
    medication_plan {
        bigint id PK
        bigint elder_id
        bigint medicine_id
        varchar_100 medicine_name
        varchar_50 dosage
        tinyint frequency
        varchar_100 time_points
        date start_date
        date end_date
        varchar_20 meal_relation
        varchar_20 status
        varchar_200 remark
        bigint created_by
        datetime create_time
        datetime update_time
        tinyint deleted
    }
    medication_task {
        bigint id PK
        bigint plan_id
        bigint elder_id
        bigint medicine_id
        varchar_100 medicine_name
        varchar_50 dosage
        varchar_20 meal_relation
        date plan_date
        datetime plan_time
        varchar_20 status
        tinyint was_missed
        datetime confirm_time
        bigint confirm_by
        varchar_200 confirm_remark
        tinyint notify_sent
        datetime notify_time
        datetime create_time
        datetime update_time
        tinyint deleted
    }
    medicine_dict ||--o{ medication_plan : "medicine_id"
    medication_plan ||--o{ medication_task : "plan_id"
```

### 3.5 五、评价、投诉与消息域

```mermaid
erDiagram
    order_review {
        bigint id PK
        bigint order_id UK
        varchar_32 order_no
        bigint family_id
        bigint elder_id
        bigint companion_id
        tinyint score
        varchar_200 tags
        varchar_500 content
        tinyint is_anonymous
        varchar_500 companion_reply
        datetime reply_time
        tinyint is_valid
        datetime create_time
        datetime update_time
        tinyint deleted
    }
    complaint {
        bigint id PK
        bigint order_id
        varchar_32 order_no
        bigint complainant_id
        varchar_20 complainant_role
        bigint target_user_id
        varchar_20 target_role
        varchar_20 type
        varchar_1000 content
        json evidence
        varchar_20 status
        bigint handle_admin_id
        varchar_500 handle_result
        tinyint penalty_to_target
        datetime handle_time
        datetime create_time
        datetime update_time
        tinyint deleted
    }
    internal_message {
        bigint id PK
        bigint receiver_id
        bigint sender_id
        varchar_30 type
        varchar_50 title
        varchar_500 content
        varchar_20 biz_type
        bigint biz_id
        varchar_200 link_url
        tinyint is_read
        datetime read_time
        tinyint receiver_deleted
        datetime create_time
        datetime update_time
        tinyint deleted
    }
```

## 四、订单状态机

`companion_order.status` 的合法流转路径。**禁止跳级、禁止回退**，
仅管理员可强制改终态（写入 `order_status_log.is_force = 1`）。

```mermaid
stateDiagram-v2
    [*] --> PENDING : 家属下单
    PENDING --> ACCEPTED : 陪诊员接单
    PENDING --> CANCELLED : 家属/管理员取消
    ACCEPTED --> IN_SERVICE : 陪诊员到院打卡
    ACCEPTED --> CANCELLED : 家属/管理员取消
    IN_SERVICE --> COMPLETED : 陪诊员完成并提交小结
    IN_SERVICE --> CANCELLED : 管理员强制终止
    COMPLETED --> REVIEWED : 家属评价
    REVIEWED --> [*]
    CANCELLED --> [*]
```

| 状态 | 枚举值 | 可执行操作 | 写入的日志 |
|---|---|---|---|
| 待接单 | `PENDING` | 陪诊员接单 / 家属取消 / 陪诊员拒单（不改状态） | `order_status_log` + `order_reject_log` |
| 已接单 | `ACCEPTED` | 陪诊员到院打卡 / 取消 | `order_status_log` + `order_checkin` |
| 服务中 | `IN_SERVICE` | 节点打卡 / 上传轨迹 / 提交小结完成 | `order_status_log` + `order_checkin` + `companion_track` |
| 已完成 | `COMPLETED` | 家属评价 / 管理员核定结算 | `order_status_log` + `companion_profile` 聚合 |
| 已评价 | `REVIEWED` | 终态 | `order_review` |
| 已取消 | `CANCELLED` | 终态 | `order_status_log`（`is_force` 标记强制） |

> 并发接单防超卖：`companion_order.version` 配 MyBatis-Plus `@Version` 乐观锁，
> 两名陪诊员同时接单只有一人 `UPDATE ... WHERE version = ?` 影响行数为 1。
