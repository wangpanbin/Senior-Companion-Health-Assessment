# M2-auth 分支代码审查报告（2026-09-17 复核修订版）

> **原报告日期**：2026-09-17
> **复核修订日期**：2026-09-17（同日交叉验证后修订）
> **原报告声明基准**：`feature/M2-auth`（M2..M9，13 commits，262 files，34,255 insertions，413 deletions），范围 `6bc37c5..42ff632`
> **实际审查对象**：`master` 工作区（见下文「〇、基线勘误」——原报告的行号与文件引用全部指向 master，而非 `feature/M2-auth`）
> **原审查方法**：mattpocock-skills:code-review 双轴并行（Standards 轴 + Spec 轴）
> **原审查工具**：Mavis（MiniMax Code）root session `mvs_98551e4113f245fd92f82ece428369c5`
> **本次复核方式**：逐条对照 HEAD 工作区代码、JUnit 测试、`plan.md`、`docs/api/*`、`AGENTS.md` 与 git 历史
> **本修订版自身的勘误**：初稿曾把 `COMPLETED → CANCELLED` 与 `REVIEWED → CANCELLED` 一并否决，属误判——`COMPLETED` 不是终态（`OrderStatus.isTerminal()` 只认 `REVIEWED`/`CANCELLED`），实现与 `docs/api/03-order.md` 图均放行该边。已在 Spec #1 分条更正（原报告的这条建议其实是对的）

---

## 〇、基线勘误（复核新增，必读）

### 0.1 变更统计数字：**全部正确** ✅

`git diff --shortstat 6bc37c5 42ff632` 输出 `262 files changed, 34255 insertions(+), 413 deletions(-)`，`git log --oneline 6bc37c5..42ff632 | wc -l` 为 `13`。原报告的这三个数字与提交数**逐字吻合**。

### 0.2 但"审查对象是 `feature/M2-auth`"是错的 ❌

原报告引用的**文件与行号全部指向 `master` 工作区**，`feature/M2-auth` 上并不存在：

| 原报告引用 | 在 `feature/M2-auth` 上的实际情况 |
|---|---|
| `OrderServiceImpl.java:316, 359, 399, 442, 480`（Standards #1 / #2 / Spec #6） | `feature/M2-auth` 的该文件只有 **995 行**（master 为 1196 行），L316 是 `}`。同样的 5 处硬编码判断在 **L298 / L341 / L381 / L424 / L462** |
| `controller/common/FileController.java`（Spec #4） | **该文件不存在**于 `feature/M2-auth`。由 `bc0e0df`（M9 文件上传）引入，在 `6bc37c5..42ff632` 范围之外 |
| `frontend/src/views/family/order-detail.vue:40, 54-56`（Standards #6） | **该文件不存在**于 `feature/M2-auth`。由 `fa45473`（双布局 + 路由全量对齐）引入，同样在范围之外 |

`git diff --shortstat feature/M2-auth master` = `167 files changed, 25892 insertions(+), 4886 deletions(-)`。

**结论**：这是一份对 `master` 工作区做的审查，头部写成了 `feature/M2-auth @ 6bc37c5..42ff632` 的地址。原报告的行号对 **master 有效**，对 `feature/M2-auth` 无效。本修订版所有行号统一以 **HEAD（master）** 为准并显式标注。

### 0.3 "合并门槛"已过期 ❌

原报告称"P0 必须修完才能合 `feature/M2-auth`"。但 `git merge-base --is-ancestor 42ff632 master` 退出码为 `0`——`feature/M2-auth` 的 tip **早已是 master 的祖先**，分支已合并且 master 大幅领先。该门槛对当前状态不再适用，P0 应作为**独立修复任务**而非合并前置条件。

---

## TL;DR（修订后）

- **原报告 findings**：13 处（1 hard + 5 smell + 3 missing + 2 creep + 2 wrong）
- **复核后处置**：**7 处留存** + **3 处撤回** + **3 处合并**
- **留存**：
  - **P0（1 处）**：订单状态机权威判断为空转——`OrderStatus.canTransitTo` 在生产代码里**调用点为 0**，且原报告实际漏列 1 处（`markReviewed`）
  - **P1（3 处）**：`/api/file/upload` 缺 api 文档、`MessageType.COMPLAINT_SUBMITTED` 未进消息文档、`docs/api/07-message.md` 与 SSE 实现漂移
  - **P2（3 处）**：`PermissionProbeController` 探针（标签用错）、`OrderServiceImpl` 低内聚、`OrderServiceImpl` 排序白名单/日期解析混装
- **撤回（3 处）**：Spec #7（"模板缺失静默 null"完全不成立）、Spec #6（论证自相矛盾 + 引用错文件）、Standards #6（雪花 ID 精度风险在代码中不存在）
- **合并（3 处）**：Standards #2 → #1；Spec #1 / Spec #2 → P0 根因
- **已确认修复方向（本轮不改代码）**：口径 A——把 `PENDING → CANCELLED` 收进 `OrderStatus.TRANSITIONS`，同步修正 `OrderStatusTest` 与 javadoc 措辞
- **注意**：原报告 Standards #1 与 Spec #1 的两条建议**互斥，字面组合无法实施**（详见 #1 的「建议的更正」）

---

## 双轴汇总（修订后）

| 轴 | 原报 findings | 复核后 |
|---|---|---|
| Standards | 1 hard + 5 smell | 1 hard + 3 smell 留存，#2 合并进 #1，#6 撤回 |
| Spec | 3 missing + 2 creep + 2 wrong | 3 处文档漂移留存（#3/#4/#5），#1/#2 重新定性为设计决策，#6/#7 撤回 |

**两轴仍在同一根因上撞车**，但原报告对该根因的定性有误：

- **正确的根因**：`OrderStatus.canTransitTo` 被 `AGENTS.md §4.1` 声明为"唯一权威判断"，但它在 main 代码里**一次都没有被调用**（6 处状态判断全是手写等值比较）。
- **原报告的错误定性**：把"`TRANSITIONS` 缺 `→CANCELLED` 边"当成 spec 缺陷。实际上该表只承载**正向流转**，排除 `CANCELLED` 是**刻意设计且有测试锁定**的（见 Spec #1）。

---

## 优先级 + 工作量矩阵（修订后）

| 优先级 | 项 | 工作量 | 来源轴 | 处置 |
|---|---|---|---|---|
| **P0** | 订单状态机权威判断落地（`canTransitTo` 生产调用点为 0）+ 补上漏列的第 6 处 | M（半天 + 回归） | Standards #1 / Spec #1 | **留存，口径 A 已确认** |
| **P1** | `/api/file/upload` 补 api 文档 | S | Spec #4 | 留存（取值需更正） |
| **P1** | `MessageType.COMPLAINT_SUBMITTED` 补文档（**不是删值**） | S | Spec #3 | 留存（建议已更正） |
| **P1** | 协调 `docs/api/07-message.md` 的 WS 描述与 SSE 实现 | S | Spec #5 | 留存 |
| **P2** | `PermissionProbeController` 整体删除（不是删 `identity` 方法） | S | Standards #5 | 留存（标签已更正） |
| **P2** | 拆 `OrderServiceImpl` 的订单号 / 定价 / 排序职责 | M | Standards #3 / #4 | 留存（标签与位置已更正） |
| ~~P3~~ | ~~`route.params.id` 显式 `String(...)` 防雪花精度~~ | — | Standards #6 | **撤回** |

---

## Standards findings

### A. 硬违反 AGENTS.md

#### #1 订单状态机绕过统一判断【P0 · 留存并扩写】

- **位置**：`backend/src/main/java/org/company/nianglin/service/impl/OrderServiceImpl.java`
  - `cancel()` L305-342，硬编码判断 **L316** `if (from != OrderStatus.PENDING)`
  - `accept()` L350-383，**L359** `if (from != OrderStatus.PENDING)`
  - `reject()` L391-423，**L399** `if (OrderStatus.of(order.getStatus()) != OrderStatus.PENDING)`
  - `start()` L431-462，**L442** `if (from != OrderStatus.ACCEPTED)`
  - `complete()` L470-529，**L480** `if (from != OrderStatus.IN_SERVICE)`
  - **`markReviewed()` L551-579，L556 / L560 —— 原报告漏列** ❌
- **现状**：共 **6 处**（原报告写 5 处）手写等值判断，全部绕过 `canTransitTo`。
- **规则**：`AGENTS.md §4.1` "`OrderStatus.canTransitTo(target)` 是唯一权威判断，所有状态流转代码**必须**通过它，**禁止**硬编码字符串。"

  > ⚠️ **原文引用勘误**：原报告 Spec #1 / #6 把该规则写成 "`plan.md §4.1`"。**引错了文件**——`plan.md` 全文没有"硬编码"三字，且 `plan.md` 的 §4.1 是"**4.1 主机**"（本地开发环境检测结果），与状态机无关。规则出处是 **`AGENTS.md §4.1`**；`plan.md` 里真正相关的是 **§三 M4（L246）**。

- **比原报告更严重的一点**：原报告称"同一规则在模块间不一致：`AdminServiceImpl:526` 走 `canTransitTo`，符合规范"——**这是误引**。`AdminServiceImpl:526` 属于 `handleComplaint()`，判断的是 **`ComplaintStatus`（投诉状态机）**，与 `OrderStatus` 无关。

  核实结论：`OrderStatus.canTransitTo` 在 `src/main` 下的**生产调用点为 0**（全仓库仅 `OrderStatusTest` 断言、`ComplaintStatus` 的同名方法、以及 `OrderStatus` 自身定义三处匹配）。所以问题不是"模块间不一致"，而是**该权威方法在订单模块从未被使用**。

- **建议的更正（重要）**：原报告把 Standards #1 与 Spec #1 当作"同一根因，一起修"，但**两条建议字面组合无法实施**：

  - 只做 Standards #1（把 `cancel()` 换成 `!from.canTransitTo(CANCELLED)`）→ `TRANSITIONS` 里没有 `PENDING→CANCELLED`，`canTransitTo` **恒为 false**，所有取消全部失败；
  - 两条都做（含 `ACCEPTED/IN_SERVICE→CANCELLED`）→ 家属将能取消**已接单**的订单，**直接违反** `OrderServiceImpl:317-318` 的注释与 `docs/api/03-order.md:122`（FAMILY 只能从 PENDING 取消）。

  **已确认口径 A**（本轮仅记录，不改代码）：

  1. `OrderStatus.TRANSITIONS` 增加一条边 `PENDING → CANCELLED`（承认系统本就存在"家属从 PENDING 取消"这条非管理员入边）；
  2. **同步修正** `OrderStatusTest:64-70`（`cancelIsNotAForwardTransition` 当前断言 `PENDING.canTransitTo(CANCELLED) == false`）与 `OrderStatus` javadoc L56 的措辞；
  3. **不要**把 `ACCEPTED / IN_SERVICE / COMPLETED / REVIEWED → CANCELLED` 加进 `TRANSITIONS`：`REVIEWED` 是终态，任何路径都进不去；其余三条属于**管理员强制路径**，由 `forceTerminal()` 放行，本就不属于"正向流转"（`08-admin.md:386` 明写强制终态绕过正向流转规则）。注意 `COMPLETED → CANCELLED` 是**被强制路径合法放行的边**（见 Spec #1 ②），只是**不进** `TRANSITIONS`；`TRANSITIONS` 只新增 `PENDING → CANCELLED` 这一条家属路径；
  4. 6 处判断（含 `markReviewed`）统一改为 `canTransitTo(target)`；`forceTerminal()` 的管理员强制路径保留 `isAdminForceable()` + `isTerminal()`，并由 `AGENTS.md §4.1` 铁律 4 明确豁免。

### B. 代码异味（judgement call）

#### #2 可能 Repeated Switches【已合并至 #1】

- 位置同 #1。实际形态是 6 处**重复的条件等值判断**（不是 `switch`）。修 #1 即顺手解决。

#### #3 低内聚：定价常量与订单号规则混在同一类【P2 · 留存，标签更正】

- **位置**：`OrderServiceImpl.java`
  - 订单号簇：常量 L104-113、Lua 脚本 L115-125、`nextOrderNo()` L946-979、`orderNoExists()` L982-986、`maxUsedSeq()` L994-1004、`raiseSeqAbove()` L1015-1017
  - 定价簇：常量 L127-133、`resolveFee()` L1026-1038
- **标签更正** ❌：原报告称之为 **Data Clumps（数据泥团）**。但 Data Clumps 指"同一组变量反复成组出现在方法参数表里"；这里的常量是**类级 `private static final`，从未作为参数成组传递**，不构成数据泥团。真实问题是**低内聚 / Divergent Change**（见 #4）。
- **建议**：抽 `OrderNoCodec`（订单号生成 / 解析 / 序列 TTL）与 `OrderPricing`（定价）两个 value object 仍然合理，但应挂在 #4 的"按变更理由拆分"名下，而不是"消除数据泥团"。

#### #4 可能 Divergent Change【P2 · 留存，位置更正】

- **位置更正** ❌：原报告写 `OrderServiceImpl.java:1090-1125` 处"既做 `DateTimeFormatter` 解析、订单号生成、又做 `applySort` 排序白名单"。实际上该区间（L1078-1123）**只有** `applyDateRange()` / `parseDate()` / `applySort()`，**不含订单号生成**（订单号在 L946-1004）。
- **成立的部分**：1196 行的 `OrderServiceImpl` 同时承担订单号发号、定价规则、日期解析、排序白名单、订单 CRUD、状态流转、通知装配，至少 3 个互不相关的变更理由。
- **建议**：
  - 订单号 → `OrderNoCodec`；定价 → `OrderPricing`（同 #3）；
  - ⚠️ **原文的排序建议与 `AGENTS.md §2.6` 冲突**：原报告建议"排序白名单迁到 `MybatisPlusConfig`"，但规范明确要求"排序字段 `sortField` 必须**白名单**，在 **Service 层**校验（防 SQL 注入 / 索引失效）"。若要抽，应抽成 Service 层内的独立 helper（如 `OrderSortWhitelist`），**不要**移出 Service 层。

#### #5 权限探针控制器待删除【P2 · 留存，标签更正】

- **位置**：`backend/src/main/java/org/company/nianglin/controller/common/PermissionProbeController.java`（全文 96 行）
- **标签更正** ❌：原报告把 `identity(gate)`（L87-95）称为 **Middle Man（中间人）**，并说"Javadoc 已自标'可整体删除'"。
  - Middle Man 指"一个类几乎只做转发"。`identity(gate)` 是**被 7 处调用的私有构造方法**，不构成 Middle Man；
  - "可整体删除"（L27-28）说的是**整个 controller**，不是这个私有方法。原报告把两者混为一谈。
- **成立的部分**：该探针不承载业务语义，M2 权限矩阵验收完成后**可以整体删除**，长期回归由 `PermissionMatrixTest` 承担。
- **建议**：开一个独立 PR，与真实越权矩阵测试同步合入并**删除整个 controller**（而非删除 `identity` 方法）。

#### #6 可能 Message Chains / ID 类型隐患【已撤回】

- **撤回理由** ❌：
  1. **前提不成立**：全项目主键是 `IdType.AUTO`（`AGENTS.md:213`、`common/BaseEntity.java:36`），**没有任何雪花 ID 计划**，仓库内搜不到 `ASSIGN_ID` / snowflake 相关配置；
  2. **风险在代码中不存在**：`frontend/src` 内搜不到任何 `Number(route.params…)` 或 `parseInt(route.params…)`；4 个 `route.params.id` 站点（`family/order-detail.vue:40`、`family/order-complaint.vue:28`、`family/order-review.vue:26`、`companion/execute.vue:32`）全部是把 string 直接交给 api 层，**不存在精度转换**；
  3. **建议本身是空操作**：`route.params.id` 本来就是 string，`String(route.params.id)` 不改变任何东西；
  4. **抽样不一致**：即便按原报告的逻辑，也应同时点出上述 4 个同类站点，而非只点 `order-detail.vue` 一个。
- 若仍想加 ESLint `no-restricted-syntax` 作为**预防性**护栏，可作为独立技术债记录，但不构成 P3 finding。

---

## Spec findings

### (a) Missing / partial

#### #1 订单状态机模型"缺边"【已重新定性】

- **位置**：`backend/src/main/java/org/company/nianglin/constant/OrderStatus.java:58-65`
- **原报告定性** ❌：把 `TRANSITIONS` 未包含 `→CANCELLED` 判为 spec 缺陷（P0），并要求补齐 5 条入边。
- **复核定性** ✅：这是**刻意的设计**，且被测试与注释双重锁定：
  - `OrderStatus.java:56` 注释明写"注意 `CANCELLED` 不在任何正向流转中 —— 取消只能由管理员的纠纷处理触发"；
  - `OrderStatusTest.java:64-70`（`cancelIsNotAForwardTransition`）**专门断言** `PENDING/ACCEPTED/IN_SERVICE` 都**不能** `canTransitTo(CANCELLED)`；
  - `forceTerminal()`（L587-630）走 `isAdminForceable()` + `isTerminal()` 这条**独立**的管理员强制路径。
  
  `canTransitTo` 的 javadoc 与 `TRANSITIONS` 的注释都限定它表达"**正向流转**"。原报告把"正向流转表"当成了"完整状态机表"。
- **原报告建议的两条边必须分开裁决** ❌/✅：

  **① `REVIEWED → CANCELLED` —— 应予否决 ❌，4 处约束一致反对：**
  1. `OrderStatus.isTerminal()`（L68-70）把 `REVIEWED` 判为终态，`forceTerminal()` L602-605 据此**直接拒绝**任何以 `REVIEWED` 为起点的强制变更；
  2. `docs/api/08-admin.md:380` 错误场景表：`3002 | 订单已是终态，不可再处理`；
  3. `docs/api/03-order.md:128` 铁律 3"**终态锁死**：`已评价` / `已取消` 之后不可再变更"，及 `AGENTS.md §4.1` 铁律 3；
  4. `OrderStatusTest.java:59-60`：`REVIEWED` / `CANCELLED` 不可 `canTransitTo` 任何状态。

  **② `COMPLETED → CANCELLED` —— 应当保留 ✅（原报告这条不算错，本修订版上一稿误判，现已更正）：**
  - 实现**放行**：`isTerminal()` 只认 `REVIEWED` / `CANCELLED`，`COMPLETED` **不是终态**，故 `forceTerminal()` 的 L602 守卫放行 `COMPLETED → CANCELLED`；
  - `docs/api/03-order.md:107-108` 的图**已包含**这条边（四列合并入 `CANCELLED`）；
  - `08-admin.md` 的 `3002` 只把"已是终态"列为拒绝条件，而 `COMPLETED` 不是终态；
  - 语义上必须保留：服务已完成但事后判定陪诊员有责时，需要「取消 + 标记线下退费」，删掉这条边会堵死该路径。

- **因此"任意"的精确含义**：`docs/api/03-order.md:121` 的"⑤ **任意** → 已取消"应当收窄为
  **`PENDING / ACCEPTED / IN_SERVICE / COMPLETED`（即 `已评价` 与 `已取消` 除外）**。
  当前该文件 L111 的图**多画了一条 `REVIEWED → CANCELLED` 箭头**，与它自己的 L128 铁律 3 直接冲突——这才是真正需要修的文档缺陷。
- **顺带确认**：`AGENTS.md §4.1` 的状态机图把 `CANCELLED` 画成只从 `PENDING/ACCEPTED/IN_SERVICE` 进入，**漏了 `COMPLETED`**，与实现不一致，应一并补画。
- **保留的结论**：真正的 P0 是 `canTransitTo` 在生产代码中未被使用，不是"缺边"。`08-admin.md:386` 已明确"强制终态**绕过状态机的正向流转规则**"，这进一步证明原报告"缺 `→CANCELLED` 边 = spec 缺陷"的前提不成立。

#### #2 `canTransitTo` 不在任何一处是唯一真源【已合并至 Standards #1】

- 与 Standards #1 同根因。定性由"缺边"更正为"**权威方法在生产代码中调用点为 0**"。

### (b) Scope creep（spec 之外的功能）

#### #3 `MessageType.COMPLAINT_SUBMITTED` 未进消息文档【P1 · 留存，建议更正】

- **位置**：`backend/src/main/java/org/company/nianglin/constant/MessageType.java:33`
- **核实**：`docs/api/07-message.md` 的**两张表都缺**这一项——§一「消息类型」表（L35-43）9 条、§三「消息模板」表（L227-235）9 条；`plan.md:354`（M8）确实只列 4 类（订单事件 / 资质审核结果 / 漏服提醒 / 系统公告）。原报告"§三 列出 9 个模板"的计数正确。
- **建议更正** ❌：原报告的**选项 A（推荐）"删除 `COMPLAINT_SUBMITTED` 枚举值，统一用 `SYSTEM_NOTICE`"是错的**——该类型是**活的、被真实调用的**：
  - `ComplaintServiceImpl.java:239` `messageService.sendBatch(adminIds, MessageType.COMPLAINT_SUBMITTED, ...)`
  - `ComplaintServiceImpl.java:248` `messageService.send(complaint.getTargetUserId(), MessageType.COMPLAINT_SUBMITTED, ...)`
  - `docs/agents/FIXTURE_ROLLBACK_PLAN.md:44` 也把它记为家属投诉流程的写入项。
  
  删值会**直接打断投诉通知链路**。应采用原报告的**选项 B**：在 `plan.md §M8` 与 `docs/api/07-message.md` §一/§三 补录该类型（触发条件、模板字段、接收方）。
- **✅ 已修复（2026-09-17）**：`docs/api/07-message.md` §一「消息类型」与 §三「消息模板」各补入 `COMPLAINT_SUBMITTED`（接收方：全部管理员 + 被投诉方；正文模板 `{submitterName} 就订单 {orderNo} 提交了投诉（{typeLabel}），请及时处理。`）；`plan.md §M8` 由"四类消息模板"改为"五类"，并补一条验收项。

#### #4 `POST /api/file/upload` 缺 api 文档【P1 · 留存，引用与取值更正】

- **位置**：`backend/src/main/java/org/company/nianglin/controller/common/FileController.java`（81 行）
- **成立的部分** ✅：`docs/api/` 下 10 个 `.md`（01-09 + README）**没有一篇**记录该端点；`docs/api/README.md` 的角色矩阵（L291-301）也**没有** `/api/file/**` 行。
- **"完全缺失 spec"属高估 ❌**：`docs/api/README.md` **§九「文件上传约定」**（L467-479）已经写清了 multipart、字段名 `file`、单文件 10 MB / 单请求 20 MB、允许类型（jpeg/png/webp/pdf）、返回 `{fileId,url,size}`、存储路径 `./uploads/{yyyyMM}/{uuid}.{ext}`、以及"必须读 magic bytes"。
  真正缺的只有两样：**端点的路径/方法/权限**，以及 **`bizType` 白名单取值**。原报告"完全缺失 spec"的措辞应更正为"未进 `docs/api/` 的模块级文档"。
- **引用更正** ❌：原报告写"`README.md §五 角色矩阵`"。**`README.md` 根本没有"角色矩阵"这一节**（其章节为：当前阶段范围 / 目录结构 / 技术栈 / 快速开始 / 端口规划 / 接口约定速查 / 合规约束 / 开发规范 / 文档索引）。角色矩阵在 **`docs/api/README.md`**。另：原报告写"`docs/api/*.md` 全套 9 篇"，实际为 **10 篇**。
- **取值更正** ❌：原报告建议列出 `bizType` 取值为 "`CERT / AVATAR / COMPLAINT_EVIDENCE / ...`"——**三个全错**。实际白名单是 `FileBizType`（`constant/FileBizType.java`）：**`COMPANION_CERT` / `COMPLAINT` / `AVATAR`**，且**刻意不放行** `CHECKIN`（打卡照片有专属入口 `POST /api/execution/{orderId}/photo`，带归属校验）与 `REVIEW`（一期评价无图片字段）。
- **补充**：该端点在 `docs/agents/FRONTEND_CONTRACT.md:215-220` 已有记录（含正确的三个取值与 multipart 字段），所以更准确的说法是"**未进 `docs/api/` 正式接口文档**"，而非"完全缺失 spec"。
- **建议**：新增 `docs/api/10-file-upload.md`，列明 `bizType` 三个白名单取值、`size` 上限、允许类型（`image/jpeg` / `image/png` / `image/webp` / `application/pdf`，与 `AGENTS.md §4.5` 对齐）、**不接收 `bizId`** 的设计理由。
- **✅ 已修复（2026-09-17）**：新建 `docs/api/10-file-upload.md`（端点、`bizType` 三值白名单及不放行 `CHECKIN`/`REVIEW` 的原因、错误场景表、验收标准）；
  `docs/api/README.md` 角色矩阵补 `/api/file/**` 行、文档索引补 `10-file-upload.md`。

#### #5 站内信推送通道 SSE vs WS【P1 · 留存】

- **核实** ✅：`docs/api/07-message.md:56` 的接口表与 L196 的说明都写 `WS /ws/message?token=`；实现侧是 SSE——`MessageSseController` / `MessageSseHub` 提供 `GET /sse/message`。原报告"§二 line 56"的位置引用准确。
- **补充**：`docs/agents/designs/M8-message.md:19` 本就按 `GET /sse/message` 设计，`docs/agents/PLAN_BACKEND.md:76` 也明确"M5 客户端连 `/ws/order-progress`，M8 客户端连 `/sse/message`"。所以这不是"未记录的偏离"，而是 **`docs/api/07-message.md` 未随设计稿同步**。
- **建议**：采纳原报告**选项 B**——修改 `docs/api/07-message.md`，把 WS 描述改为 `GET /sse/message`（含 `Last-Event-ID` 重连、消息格式、鉴权走 `Authorization` 头），与 `docs/agents/designs/M8-message.md` 对齐。选项 A（改回 WebSocket）不必要。
- **✅ 已修复（2026-09-17）**：`docs/api/07-message.md` §二接口表第 6 行与 §6 整节由 WS 改为 **SSE**：
  `GET /sse/message`、鉴权走 `Authorization` 头、事件名 `NEW_MESSAGE`、`data` 为 JSON 字符串、连接超时 30 分钟。
  并补充了两条实现真相：端点**刻意不带 `/api` 前缀**（避免被 Axios 30 秒超时掐断）；
  **原生 `EventSource` 无法自定义请求头**，故一期前端实际走 60 秒轮询兜底（`frontend/src/api/message.js`）。
  `plan.md §M8` 也同步由"WebSocket 推送"改为 SSE 口径。

### (c) Wrong / suspicious

#### #6 `OrderServiceImpl.cancel` 与自身状态机自相矛盾【已撤回并重写】

- **撤回理由** ❌：原报告 L175 的论证为"将来把 `CANCELLED` 加入 `PENDING.TRANSITIONS` 后，`canTransitTo` 会返 `false`（因为当前 `from != PENDING` 是判断条件而非 status.transit 关系），取消路径不会触发"。
  
  这段论证**自相矛盾**：当前 `cancel()`（L315-320）**根本没有调用** `canTransitTo`，修改 `TRANSITIONS` 不会影响它的行为；"取消路径不会触发"也就无从谈起。`canTransitTo` 与 `from != PENDING` 是两个独立判断，不存在"因为…所以返 false"的因果。
  
  其引用的"`plan §4.1` 明文：禁止硬编码状态字符串"同样**引错文件**（应为 `AGENTS.md §4.1`，见 Standards #1 的引用勘误）。
- **重写后的有效表述**：`cancel()` 与 `accept/start/complete` 一样，属于 Standards #1 的 6 处硬编码之一；其正确修法取决于状态机口径（已确认口径 A），**本身不构成独立的 finding**。

#### #7 `MessageType.COMPLAINT_SUBMITTED` 无对应模板【已撤回】

- **撤回理由** ❌：该 finding 的事实陈述**完全不成立**，核实如下：
  1. `MessageTemplateUtil` 对 `COMPLAINT_SUBMITTED` 的模板**三处齐全**：`title()` L52、`content()` L90、`linkUrl()` L116（跳 `/admin?complaintId=`）；
  2. 三处都是**穷尽 `switch`**，**没有 `default` 分支**——未注册的枚举类型在 Java 里**编译失败**，运行时不可能出现"找不到模板"；
  3. 代码里**搜不到** `Enums.stream(MessageType.values())`，也**搜不到** "message dispatched but template missing" 之类日志；
  4. `MessageServiceImpl.send()` L219-224 唯一的静默 `return null` 条件是 `receiverId == null || type == null`，且注释明确说明这是"收件人不存在是业务上的正常情况（例如订单还没有陪诊员）"的**刻意设计**，与"模板缺失"无关；
  5. 所谓"dashboard 红点计数对不上"无任何代码或日志依据。
- **处置**：整条撤回。该类型的真实问题只有"未进文档"这一条（即 Spec #3）。

---

## Spot-check 通过项（复核确认无误 ✅）

下列项经复核**全部准确**，无需调整：

- 37 处 `@Transactional(rollbackFor = Exception.class)`（`service/impl/*.java`，满足"30+ 处"）
- `@PreAuthorize` 按意图配齐
- ELDER 只读由 `ElderReadOnlyInterceptor` 兜底（文件存在，`WebMvcConfig#addInterceptors` 对 `/api/**` 非 GET 默认拒绝）
- `UserInfoVO.java:103` 走 `MaskUtil.phone`
- `localStorage` key 全部归口 `auth.js`
- 文件上传读 magic bytes 后才信前端 `Content-Type`（`FileStorageServiceImpl` 类注释 L28-31 明写只认前几个字节）
- `GlobalExceptionHandler:175` 处理 `MaxUploadSizeExceededException` 干净
- Controller 层未发现 `try/catch → Result.fail` 反模式
- 错误码段位（1xxx..9xxx）业务归属正确

---

## 与原始 raw 报告对齐

原报告与以下两份原始 sub-agent 输出语义对齐（精简 + 排版）：

- **Standards 轴 raw**：`mavis` session `mvs_9e82a531a0ce47f2af252f2e5b6b11ee` / task `bg_93684216-1220-42f1-9fb4-156c6f293513`
- **Spec 轴 raw**：`mavis` session `mvs_9213c95816f54581aba9c1ceac21238c` / task `bg_e0c0bbb3-fbca-43af-a96b-5d1568fea5a1`

> ⚠️ 复核发现原报告的基线标注（`feature/M2-auth @ 6bc37c5..42ff632`）与实际审查对象（master 工作区）不符，故上述两份 raw 输出所依据的代码快照也需按 **master** 重新理解。如需引用 raw 原文找回上下文，请一并核对其代码基准。

---

## 后续行动建议（修订后）

### ✅ 本轮已完成：文档一致性修复（2026-09-17）

> 原则：**只让文档对齐已实现的代码行为**，不改任何业务代码。因此下列改动都是"文档追上现状"，不引入新的行为承诺。

**A. 核心：订单状态机"任意 → 已取消"的边界（跨 8 个文件）**

统一为：**`PENDING / ACCEPTED / IN_SERVICE / COMPLETED` 可进入 `CANCELLED`；`REVIEWED` / `CANCELLED` 是终态，无出边。**
其中**家属**只有 `PENDING → CANCELLED` 一条路径，其余全部属于**管理员强制**（`forceTerminal`，`08-admin.md` 记为"绕过状态机的正向流转规则"）。

| 文件 | 改动 |
|---|---|
| `AGENTS.md` §4.1 | 状态机图补 `COMPLETED → CANCELLED` 边；补"两条取消路径"说明；**删除不实的"实现已就绪"声明**，改为如实标注 `canTransitTo` 生产调用点为 0，并明确管理员强制是**豁免的独立路径**、其边不进 `TRANSITIONS` |
| `docs/api/03-order.md` §二 | 删除**多画的 `REVIEWED → CANCELLED` 箭头**；"⑤ 任意 → 已取消"精确化为四态并补终点 `COMPLETED`；铁律补第 4/5 条（家属取消 vs 管理员强制；终态锁对强制路径同样生效） |
| `docs/api/08-admin.md` §9 | 实现要点补"起点限任意非终态"、明确 **`COMPLETED → CANCELLED` 合法**、`3002` 场景写明为 `REVIEWED`/`CANCELLED`；验收清单补一条终态拒绝用例 |
| `docs/api/README.md` §6.1 | 状态机图补 `COMPLETED` 竖线；补取消双路径说明 |
| `docs/prd/PRD.md` | 两处"取消路径 `PENDING/ACCEPTED → CANCELLED`"更正为"家属仅 `PENDING`；管理员强制四态" |
| `docs/agents/designs/M4-order.md` §3 | 图由"PENDING+ACCEPTED 合并 cancel"更正为"**家属仅 PENDING**"；补 `forceTerminal` 的起点/终点 |
| `docs/agents/designs/M9-admin.md` §3.3 | 把"任意非终态"显式展开为四态，并写明 `REVIEWED`/`CANCELLED` → `3002` |
| `backend/sql/tools/gen_db_docs.py` + `docs/db/01-ER图.md` | mermaid 状态图补 `COMPLETED --> CANCELLED`；`ACCEPTED --> CANCELLED` 由"家属/管理员取消"更正为"管理员强制取消（家属不可）"；状态表同步。**改生成器后重新生成**（`docs/db/*` 禁止手改） |

**B. 三个 P1 文档漂移**

| 文件 | 改动 |
|---|---|
| `docs/api/10-file-upload.md` | **新建**：补录已实现但未进 `docs/api/` 的 `POST /api/file/upload`（端点、`bizType` 三值白名单及不放行 `CHECKIN`/`REVIEW` 的原因、错误场景、验收标准） |
| `docs/api/README.md` | 角色矩阵补 `/api/file/**` 行；文档索引补 `10-file-upload.md` |
| `docs/api/07-message.md` | §一/§三 补 `COMPLAINT_SUBMITTED`；§二第 6 行与 §6 整节由 WS 改为 **SSE**（`GET /sse/message`、事件 `NEW_MESSAGE`、鉴权头、30 分钟超时），并如实说明**原生 `EventSource` 无法带头、一期实际走轮询兜底** |
| `plan.md` §M8 | "四类消息模板"→"五类"（补投诉事件）；推送口径 WebSocket → SSE；补一条投诉通知验收项 |

### ⏳ 仍需处理

- **P0 代码修复（口径 A，未动代码）**：`TRANSITIONS` 增加 `PENDING → CANCELLED` → 同步修 `OrderStatusTest:64-70` 与 javadoc L56 → 6 处判断（含 `markReviewed` L556/560）改走 `canTransitTo` → `forceTerminal` 保留并已在上方文档中注明豁免。
  - 回归：`OrderStatusTest` 覆盖 `canTransitTo` 全边（该测试**需要改断言**，不是"跑一下就行"）；e2e 加一组"取消订单：家属 PENDING 成功 / 家属 ACCEPTED 失败 / 管理员四态强制 / 终态拒绝"用例。
- **下迭代清场（3 个 P2）**：删除 `PermissionProbeController`（整个类）；抽 `OrderNoCodec` / `OrderPricing`；排序白名单抽成 **Service 层内**的 helper（**不要**移出 Service 层）。
- **已撤回、不再排期**：原 Standards #6（雪花 ID 精度）、Spec #6（cancel 自相矛盾）、Spec #7（模板缺失）。
- **待人工确认**：`docs/agents/designs/` 与 `docs/prd/` 未被 `AGENTS.md §9` 的同步清单覆盖，本轮一并改了；若团队认为设计稿/PRD 应走独立评审流程，可回退这两个目录。

---

## 元信息

- **原报告作者**：Mavis（orchestrator agent）
- **原报告生成时间**：2026-09-17 11:21 GMT+8
- **本复核修订**：2026-09-17，基于 HEAD（master）工作区逐条交叉验证
- **复核结论**：13 处 findings → **7 留存 / 3 撤回 / 3 合并**；原报告的分支基线、行号、多处文件与章节引用需按本修订版更正后方可作为排期依据
- **协作约定**：保留 `mattpocock-skills:code-review` 双轴范式——Standards 轴与 Spec 轴不跨轴排名、不合并
